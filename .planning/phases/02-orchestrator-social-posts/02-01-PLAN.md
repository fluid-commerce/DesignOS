---
phase: 02-orchestrator-social-posts
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/social/quote.html
  - templates/social/app-highlight.html
  - templates/social/partner-alert.html
  - templates/social/problem-first.html
  - templates/social/stat-proof.html
  - templates/social/manifesto.html
  - templates/social/feature-spotlight.html
  - templates/social/index.html
autonomous: true
requirements: [TMPL-01, TMPL-02, SOCL-07]

must_haves:
  truths:
    - "7 social post template HTML files exist in templates/social/"
    - "Each template renders at correct dimensions in a browser (1080x1080 or 1340x630)"
    - "templates/social/index.html shows live iframe previews with content slot specs and creation instructions"
    - "Templates use assets/ paths (not Reference/ or localhost Figma MCP URLs)"
    - "Templates serve as 5-star reference examples the orchestrator subagents can study"
  artifacts:
    - path: "templates/social/index.html"
      provides: "Jonathan's format template index with iframe previews + slot specs + creation instructions"
      min_lines: 100
    - path: "templates/social/quote.html"
      provides: "Testimonial/quote archetype template"
      min_lines: 30
    - path: "templates/social/app-highlight.html"
      provides: "App highlight archetype template"
      min_lines: 30
    - path: "templates/social/partner-alert.html"
      provides: "Partner alert archetype template (LinkedIn 1340x630)"
      min_lines: 30
    - path: "templates/social/problem-first.html"
      provides: "Problem-first / pain post archetype template (orange accent)"
      min_lines: 30
    - path: "templates/social/stat-proof.html"
      provides: "Stat / proof post archetype template (green accent)"
      min_lines: 30
    - path: "templates/social/manifesto.html"
      provides: "Manifesto / brand voice archetype template (blue accent)"
      min_lines: 30
    - path: "templates/social/feature-spotlight.html"
      provides: "Feature spotlight archetype template"
      min_lines: 30
  key_links:
    - from: "templates/social/index.html"
      to: "templates/social/*.html"
      via: "iframe src attributes"
      pattern: "<iframe src=\"[a-z-]+\\.html\""
    - from: "templates/social/*.html"
      to: "assets/"
      via: "local asset paths or base64 embedded"
      pattern: "assets/|data:image"
---

<objective>
Create the 7 social post archetype templates in Jonathan's format (live HTML preview + content slot specs + creation instructions) and the template index page.

Purpose: Templates serve as 5-star reference examples that the orchestrator's subagents study to understand Fluid's visual language. They also provide the `--template` mode where subagents follow a template's structure closely.

Output: 8 files in `templates/social/` -- 7 archetype templates + 1 index page.
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-orchestrator-social-posts/02-CONTEXT.md
@.planning/phases/02-orchestrator-social-posts/02-RESEARCH.md

Source material for templates (read these during execution):
@Reference/Brand reference material/Templates/Social Post Templates/index.html
@Reference/Brand reference material/Generated Examples/Social Posts/base.css

Brand docs for accurate styling:
@brand/design-tokens.md
@brand/social-post-specs.md
@brand/asset-usage.md
@brand/layout-archetypes.md
@brand/voice-rules.md
@patterns/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adapt 3 existing archetype templates from Reference</name>
  <files>
    templates/social/quote.html,
    templates/social/app-highlight.html,
    templates/social/partner-alert.html
  </files>
  <action>
Create `templates/social/` directory and build 3 self-contained HTML template files by adapting the existing templates in `Reference/Brand reference material/Templates/Social Post Templates/templates/`.

Read the 3 existing templates to understand Jonathan's HTML structure. Adapt them into `templates/social/` with these changes:
- Replace all `localhost:3845` Figma MCP asset URLs with local `assets/` relative paths (brushstrokes, circles, logos, fonts)
- Replace any `filter: hue-rotate()` circle recoloring with CSS `mask-image` + `backgroundColor` approach per `brand/asset-usage.md`
- Keep the same visual design and structure

**quote.html**: Testimonial/quote archetype (Instagram 1080x1080)
**app-highlight.html**: App highlight archetype (Instagram 1080x1080)
**partner-alert.html**: Partner alert archetype (LinkedIn 1340x630)

**All templates must:**
- Use `body { width: Xpx; height: Ypx; margin: 0; overflow: hidden; }` for correct dimensions
- Include `<!-- target: WxH -->` dimension comment
- Include `@font-face` declarations for flfontbold.ttf and Inter-VariableFont.ttf from `assets/fonts/`
- Use brand colors from `brand/design-tokens.md` social palette
- Include standard footer structure from `patterns/index.html` (flag icon + We-Commerce wordmark left, Fluid dots right)
- Use brushstroke textures with `mix-blend-mode: screen`, `opacity: 0.10-0.25`, positioned to edge-bleed
- Use circle sketch via CSS `mask-image` + `backgroundColor` (NOT hue-rotate)
- Reference assets via relative paths: `../../assets/brushstrokes/`, `../../assets/circles/masks/`, `../../assets/logos/`, `../../assets/fonts/`
- Be completely self-contained (inline CSS, no external stylesheets)
- Include content slot comments: `<!-- SLOT: HEADLINE -->`, `<!-- SLOT: BODY -->`, `<!-- SLOT: TAGLINE -->`, etc.
  </action>
  <verify>
    <automated>ls templates/social/quote.html templates/social/app-highlight.html templates/social/partner-alert.html 2>/dev/null | wc -l | grep -q "3" && echo "PASS: 3 existing templates" || echo "FAIL: missing templates"; for f in templates/social/quote.html templates/social/app-highlight.html templates/social/partner-alert.html; do grep -q "SLOT:" "$f" && grep -q "font-face" "$f" || echo "FAIL: $f missing slots or fonts"; done; grep -rl "Reference/" templates/social/quote.html templates/social/app-highlight.html templates/social/partner-alert.html 2>/dev/null && echo "FAIL: Reference/ paths found" || echo "PASS: no Reference/ paths"; grep -rl "localhost" templates/social/quote.html templates/social/app-highlight.html templates/social/partner-alert.html 2>/dev/null && echo "FAIL: localhost URLs found" || echo "PASS: no localhost URLs"; grep -rl "hue-rotate" templates/social/quote.html templates/social/app-highlight.html templates/social/partner-alert.html 2>/dev/null && echo "FAIL: hue-rotate found" || echo "PASS: no hue-rotate"</automated>
  </verify>
  <done>3 HTML template files adapted from Reference/ exist in templates/social/, each with correct dimensions, brand styling, content slot comments, embedded fonts, proper asset paths, no Reference/ or localhost URLs, and no hue-rotate</done>
</task>

<task type="auto">
  <name>Task 2: Create 4 new archetype templates from generated examples</name>
  <files>
    templates/social/problem-first.html,
    templates/social/stat-proof.html,
    templates/social/manifesto.html,
    templates/social/feature-spotlight.html
  </files>
  <action>
Study the 28 generated examples in `Reference/Brand reference material/Generated Examples/Social Posts/` to identify the best exemplar for each archetype. Build each as a complete, polished template:

**problem-first.html**: Pain post (Instagram 1080x1080). Orange accent. Full-bleed headline stating pain. Circle sketch wraps one pain word. Body: one sentence pivoting to implication. FLFont tagline at bottom. Best exemplars: 01-connect-3am-problem variants, 05-payments-lose-a-career variants.

**stat-proof.html**: Stat/proof post (Instagram 1080x1080). Green accent. Giant stat number as hero element. Supporting context line. FLFont tagline. Best exemplars: 04-payments-acceptance-rate variants, 08-payments-94-percent variants.

**manifesto.html**: Brand voice / manifesto (Instagram 1080x1080). Blue accent. Centered bold statement. Minimal elements. Trust/reliability messaging. Best exemplars: 03-connect-trust variants, 10-every-transaction-matters variants.

**feature-spotlight.html**: Feature spotlight (Instagram 1080x1080). Purple accent. Single feature capability with visual diagram or icon area. Clean layout showing product benefit. Best exemplars: 06-payments-6x-retry variants.

**All templates must** follow the same standards as Task 1:
- Use `body { width: Xpx; height: Ypx; margin: 0; overflow: hidden; }` for correct dimensions
- Include `<!-- target: WxH -->` dimension comment
- Include `@font-face` declarations for flfontbold.ttf and Inter-VariableFont.ttf from `assets/fonts/`
- Use brand colors from `brand/design-tokens.md` social palette
- Include standard footer structure from `patterns/index.html` (flag icon + We-Commerce wordmark left, Fluid dots right)
- Use brushstroke textures with `mix-blend-mode: screen`, `opacity: 0.10-0.25`, positioned to edge-bleed
- Use circle sketch via CSS `mask-image` + `backgroundColor` (NOT hue-rotate)
- Reference assets via relative paths: `../../assets/brushstrokes/`, `../../assets/circles/masks/`, `../../assets/logos/`, `../../assets/fonts/`
- Be completely self-contained (inline CSS, no external stylesheets)
- Include content slot comments: `<!-- SLOT: HEADLINE -->`, `<!-- SLOT: BODY -->`, `<!-- SLOT: TAGLINE -->`, etc.

IMPORTANT: These are new designs derived from studying the generated examples for inspiration. Do NOT copy HTML from Reference/ -- build fresh, brand-correct templates. The final files must contain zero references to Reference/ or localhost.
  </action>
  <verify>
    <automated>ls templates/social/problem-first.html templates/social/stat-proof.html templates/social/manifesto.html templates/social/feature-spotlight.html 2>/dev/null | wc -l | grep -q "4" && echo "PASS: 4 new templates" || echo "FAIL: missing templates"; for f in templates/social/problem-first.html templates/social/stat-proof.html templates/social/manifesto.html templates/social/feature-spotlight.html; do grep -q "SLOT:" "$f" && grep -q "font-face" "$f" && grep -q "target:" "$f" || echo "FAIL: $f missing slots, fonts, or dimension comment"; done; grep -rl "Reference/" templates/social/problem-first.html templates/social/stat-proof.html templates/social/manifesto.html templates/social/feature-spotlight.html 2>/dev/null && echo "FAIL: Reference/ paths found" || echo "PASS: no Reference/ paths"; grep -rl "localhost" templates/social/problem-first.html templates/social/stat-proof.html templates/social/manifesto.html templates/social/feature-spotlight.html 2>/dev/null && echo "FAIL: localhost URLs found" || echo "PASS: no localhost URLs"; grep -rl "hue-rotate" templates/social/problem-first.html templates/social/stat-proof.html templates/social/manifesto.html templates/social/feature-spotlight.html 2>/dev/null && echo "FAIL: hue-rotate found" || echo "PASS: no hue-rotate"</automated>
  </verify>
  <done>4 new HTML template files exist in templates/social/, each with correct dimensions, brand styling, content slot comments, embedded fonts, proper asset paths, no Reference/ or localhost URLs, and no hue-rotate</done>
</task>

<task type="auto">
  <name>Task 3: Create template index page in Jonathan's format</name>
  <files>templates/social/index.html</files>
  <action>
Create `templates/social/index.html` following Jonathan's format exactly as seen in `Reference/Brand reference material/Templates/Social Post Templates/index.html`.

The index page contains:
1. A header/title section identifying this as the Social Post Template Library
2. For each of the 7 archetypes, a template row containing:
   - **Left side**: Template number + name label, then a canvas-outer div with an iframe loading the template HTML (`src="quote.html"` etc.), scaled to fit with CSS transform
   - **Right side**: A spec panel with:
     - Description paragraph (what the archetype is for, mood, accent color)
     - "Content Slots" table: Slot name, Spec (font/size/weight/constraints), Color
     - "To Create a New Version" table: Numbered creation instructions

Adapt the CSS styling from the Reference index.html (dark background, clean layout, readable spec tables). Do NOT copy content from Reference -- write fresh specs based on the actual templates created in Tasks 1 and 2.

**Template order in index:**
01 - Quote / Testimonial
02 - App Highlight
03 - Partner Alert (LinkedIn)
04 - Problem-First / Pain Post
05 - Stat / Proof Post
06 - Manifesto / Brand Voice
07 - Feature Spotlight

Each content slot table must list ALL slots for that template with exact typographic specs (font family, weight, size range, line-height, case transform, color token).

Each "To Create a New Version" must have 4-6 numbered steps specific to that archetype.
  </action>
  <verify>
    <automated>test -f templates/social/index.html && grep -c "iframe" templates/social/index.html | grep -q "7" && grep -c "Content Slots" templates/social/index.html | grep -q "7" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>templates/social/index.html exists with 7 iframe previews, 7 content slot tables, and 7 creation instruction tables in Jonathan's format</done>
</task>

</tasks>

<verification>
- All 8 files exist in templates/social/
- `ls templates/social/*.html | wc -l` returns 8
- No references to `Reference/` or `localhost` in any template: `grep -r "Reference/\|localhost" templates/social/` returns nothing
- No `hue-rotate` for circle recoloring: `grep -r "hue-rotate" templates/social/` returns nothing
- Each template has dimension comment: `grep -r "target:" templates/social/*.html | grep -v index`
- Index page has 7 iframes: `grep -c "iframe" templates/social/index.html`
</verification>

<success_criteria>
- 7 archetype templates render at correct dimensions when opened in a browser
- index.html shows live previews with slot specs and creation instructions
- All asset paths point to local assets/ (no Figma MCP URLs, no Reference/ paths)
- Circle recoloring uses mask-image approach (no hue-rotate)
- Templates are ready to serve as 5-star references for the orchestrator subagents
</success_criteria>

<output>
After completion, create `.planning/phases/02-orchestrator-social-posts/02-01-SUMMARY.md`
</output>
