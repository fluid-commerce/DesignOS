---
phase: 07-merge-jonathan-s-codebase-into-fluid-designos
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - canvas/src/lib/db.ts
  - canvas/src/lib/campaign-types.ts
  - canvas/src/lib/slot-schema.ts
  - canvas/src/server/db-api.ts
  - canvas/src/__tests__/db.test.ts
  - canvas/package.json
  - canvas/.gitignore
autonomous: true
requirements:
  - MRGR-01
  - MRGR-02
  - MRGR-03
must_haves:
  truths:
    - "SQLite database creates all tables on first access with FK constraints enforced"
    - "TypeScript types define the full Campaign > Asset > Frame > Iteration hierarchy"
    - "Slot schema types faithfully port Jonathan's field config format to TypeScript"
    - "Database API provides sync CRUD for all 5 tables (campaigns, assets, frames, iterations, annotations)"
  artifacts:
    - path: "canvas/src/lib/db.ts"
      provides: "better-sqlite3 singleton with WAL mode, schema init"
      contains: "pragma.*WAL"
    - path: "canvas/src/lib/campaign-types.ts"
      provides: "Campaign, Asset, Frame, Iteration, Annotation interfaces"
      exports: ["Campaign", "Asset", "Frame", "Iteration"]
    - path: "canvas/src/lib/slot-schema.ts"
      provides: "SlotSchema, SlotField, TextField, ImageField, DividerField types"
      exports: ["SlotSchema", "SlotField"]
    - path: "canvas/src/server/db-api.ts"
      provides: "All DB read/write functions for campaign hierarchy"
      exports: ["createCampaign", "getCampaigns", "getAssets", "getFrames", "getIterations"]
  key_links:
    - from: "canvas/src/server/db-api.ts"
      to: "canvas/src/lib/db.ts"
      via: "getDb() import"
      pattern: "import.*getDb.*from.*db"
    - from: "canvas/src/server/db-api.ts"
      to: "canvas/src/lib/campaign-types.ts"
      via: "type imports for return values"
      pattern: "import.*Campaign.*from.*campaign-types"
---

<objective>
Wave 0 Foundation: SQLite schema, TypeScript types, slot schema port, and database API layer.

Purpose: This is the single-agent foundation that all 4 agent team members depend on. It establishes the data contracts, database schema, and API functions that Teammates B (Navigation), C (Editor), and D (Agents/MCP) consume. No parallel work can begin until this plan completes.

Output: Database singleton, 5-table schema, full TypeScript type system, slot schema types (ported from Jonathan's JS config format), and a complete synchronous DB API layer.

Team Role: Single-agent execution (Wave 0 lead). Produces contracts for all teammates.
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-CONTEXT.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-RESEARCH.md

@canvas/src/lib/types.ts
@canvas/vite.config.ts
@canvas/package.json

<interfaces>
<!-- Jonathan's field config format to port (from editor.js TEMPLATES config) -->
<!-- Each template has: { fields: [...], brush: 'selector', label: 'Template Name' } -->
<!-- Field types: { type: 'text'|'image'|'divider', sel: 'CSS selector', label: 'Field Name', mode: 'text'|'pre'|'br', rows?: N } -->
<!-- Image fields: { type: 'image', sel: 'selector', label: 'Image', dims: '353 x 439px' } -->
<!-- Divider fields: { type: 'divider', label: 'Slide 01 - Cover' } -->

From canvas/src/lib/types.ts:
```typescript
export type VariationStatus = 'winner' | 'rejected' | 'final' | 'unmarked';
export const ASSET_DIMENSIONS: Record<string, { width: number; height: number }>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install better-sqlite3 and create types + slot schema</name>
  <files>canvas/package.json, canvas/src/lib/campaign-types.ts, canvas/src/lib/slot-schema.ts, canvas/.gitignore</files>
  <behavior>
    - campaign-types exports Campaign, Asset, Frame, Iteration, CampaignAnnotation interfaces matching the SQLite schema from RESEARCH.md
    - Campaign has id, title, channels (string[]), createdAt, updatedAt
    - Asset has id, campaignId, title, assetType, frameCount, createdAt
    - Frame has id, assetId, frameIndex, createdAt
    - Iteration has id, frameId, iterationIndex, htmlPath, slotSchema, aiBaseline, userState, status (VariationStatus), source ('ai'|'template'), templateId, createdAt
    - CampaignAnnotation has id, iterationId, type ('pin'|'sidebar'), author, text, x, y, createdAt
    - SlotSchema exports SlotField (TextField | ImageField | DividerField), SlotSchema interface with fields, brush, brushLabel, carouselCount, width, height
    - FieldMode type is 'text' | 'pre' | 'br'
  </behavior>
  <action>
    1. Run `cd canvas && npm install better-sqlite3 @types/better-sqlite3` to add the SQLite dependency.
    2. Add `fluid.db`, `fluid.db-wal`, `fluid.db-shm` to canvas/.gitignore.
    3. Create `canvas/src/lib/campaign-types.ts` with all interfaces for the Campaign > Asset > Frame > Iteration hierarchy. Use the schema from 07-RESEARCH.md Pattern 2 as reference. Import VariationStatus from './types'. Channels stored as string[] in TypeScript (serialized to JSON in SQLite). All timestamps as number (Unix ms).
    4. Create `canvas/src/lib/slot-schema.ts` porting Jonathan's field config format to TypeScript. Use the exact type definitions from 07-RESEARCH.md Pattern 3. Export: FieldMode, TextField, ImageField, DividerField, SlotField (union), SlotSchema.
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS/canvas && npx tsc --noEmit src/lib/campaign-types.ts src/lib/slot-schema.ts 2>&1 | head -20</automated>
  </verify>
  <done>better-sqlite3 installed, campaign-types.ts and slot-schema.ts compile with no errors, .gitignore updated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SQLite database singleton, schema init, and full DB API</name>
  <files>canvas/src/lib/db.ts, canvas/src/server/db-api.ts, canvas/src/__tests__/db.test.ts</files>
  <behavior>
    - getDb() returns a singleton Database instance with WAL mode, FK constraints ON, synchronous NORMAL
    - initSchema() creates all 5 tables (campaigns, assets, frames, iterations, annotations) with proper FK references
    - createCampaign({ title, channels }) returns a Campaign with generated nanoid
    - getCampaigns() returns Campaign[] ordered by created_at DESC
    - getCampaign(id) returns Campaign | undefined
    - createAsset({ campaignId, title, assetType, frameCount }) returns Asset
    - getAssets(campaignId) returns Asset[]
    - createFrame({ assetId, frameIndex }) returns Frame
    - getFrames(assetId) returns Frame[]
    - createIteration({ frameId, iterationIndex, htmlPath, slotSchema?, source, templateId? }) returns Iteration
    - getIterations(frameId) returns Iteration[] ordered by iteration_index ASC
    - updateIterationStatus(id, status) updates status column
    - updateIterationUserState(id, userState) updates user_state column
    - createAnnotation({ iterationId, type, author, text, x?, y? }) returns CampaignAnnotation
    - getAnnotations(iterationId) returns CampaignAnnotation[]
    - FK constraint prevents inserting asset with nonexistent campaign_id
    - Transaction wrapper for creating campaign + assets + frames atomically
  </behavior>
  <action>
    1. Create `canvas/src/lib/db.ts` following 07-RESEARCH.md Pattern 1 exactly. Use module-level singleton with guard. DB_PATH resolves to `canvas/fluid.db`. initSchema() uses CREATE TABLE IF NOT EXISTS for all 5 tables with the schema from RESEARCH.md Pattern 2. Add `optimizeDeps: { exclude: ['better-sqlite3'] }` to vite.config.ts.
    2. Create `canvas/src/server/db-api.ts` with all CRUD functions. Use nanoid for ID generation (already in package.json). Channels serialize to/from JSON. slotSchema, aiBaseline, userState serialize to/from JSON. All functions are synchronous (better-sqlite3 sync API). Include a transaction wrapper `createCampaignWithAssets(campaign, assets)` per RESEARCH.md code example.
    3. Create `canvas/src/__tests__/db.test.ts` testing: schema creation, CRUD for each table, FK constraint enforcement (inserting asset with bad campaign_id throws), transaction rollback on error, getIterations ordering.
    4. Update `canvas/vite.config.ts` to add `optimizeDeps: { exclude: ['better-sqlite3'] }`.
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS/canvas && npx vitest run src/__tests__/db.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Database singleton creates tables on first access, all CRUD functions work, FK constraints enforced, tests pass</done>
</task>

</tasks>

<verification>
- `cd canvas && npx tsc --noEmit` passes (no type errors in new files)
- `cd canvas && npx vitest run src/__tests__/db.test.ts` all green
- campaign-types.ts exports Campaign, Asset, Frame, Iteration, CampaignAnnotation
- slot-schema.ts exports SlotSchema, SlotField, TextField, ImageField, DividerField, FieldMode
- db-api.ts exports all CRUD functions
- better-sqlite3 in package.json dependencies
</verification>

<success_criteria>
All 5 SQLite tables create on first getDb() call. Full CRUD API for campaign hierarchy. TypeScript types compile. Tests pass. Downstream teammates (B, C, D) can import types and call API functions.
</success_criteria>

<output>
After completion, create `.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-01-SUMMARY.md`
</output>
