---
phase: 07-merge-jonathan-s-codebase-into-fluid-designos
plan: 06
type: execute
wave: 2
depends_on: ["07-02", "07-03", "07-04", "07-05"]
files_modified:
  - canvas/src/App.tsx
  - canvas/src/components/AppShell.tsx
  - canvas/src/components/TemplateGallery.tsx
  - canvas/src/components/TemplateCustomizer.tsx
  - canvas/src/hooks/useFileWatcher.ts
  - canvas/src/index.css
autonomous: false
requirements:
  - MRGR-05
  - MRGR-06
  - MRGR-08
  - MRGR-14
must_haves:
  truths:
    - "App.tsx uses AppShell layout with campaign navigation as the main content area"
    - "Left sidebar renders PromptSidebar, right sidebar renders ContentEditor"
    - "Template gallery appears inside the 'New' creation flow (not a top-level section)"
    - "Selecting an iteration opens the right sidebar with ContentEditor populated from slot schema"
    - "Full end-to-end flow works: see campaigns -> drill down -> select iteration -> edit in sidebar"
  artifacts:
    - path: "canvas/src/App.tsx"
      provides: "Rewired App entry point using AppShell + campaign navigation"
    - path: "canvas/src/components/TemplateGallery.tsx"
      provides: "Updated gallery appearing in creation flow"
  key_links:
    - from: "canvas/src/App.tsx"
      to: "canvas/src/components/AppShell.tsx"
      via: "renders AppShell as root layout"
      pattern: "AppShell"
    - from: "canvas/src/App.tsx"
      to: "canvas/src/store/campaign.ts"
      via: "reads currentView to determine main content"
      pattern: "useCampaignStore"
    - from: "canvas/src/components/ContentEditor.tsx"
      to: "canvas/src/lib/template-configs.ts"
      via: "loads template SlotSchema when source is template"
      pattern: "getTemplateSchema"
---

<objective>
Wave 2 Integration: Wire all teammate outputs into a working application.

Purpose: Connects Teammate B's navigation UI, Teammate C's content editor, Teammate D's MCP tools and template configs, and Teammate A's API layer into a unified, working application. This is the critical integration wave where the separate file domains merge into a cohesive user experience.

Output: Working end-to-end flow: campaign dashboard -> drill down -> select iteration -> edit in right sidebar -> save changes. Template creation flow integrated. File watcher updated for campaign model.

Team Role: Integration wave -- single agent or coordinated. Touches files from multiple teammate domains (App.tsx is shared territory).
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-CONTEXT.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-01-SUMMARY.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-02-SUMMARY.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-03-SUMMARY.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-04-SUMMARY.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-05-SUMMARY.md

@canvas/src/App.tsx
@canvas/src/components/AppShell.tsx
@canvas/src/components/ContentEditor.tsx
@canvas/src/store/campaign.ts
@canvas/src/store/editor.ts
@canvas/src/lib/template-configs.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire App.tsx to use AppShell + campaign navigation</name>
  <files>canvas/src/App.tsx, canvas/src/components/TemplateGallery.tsx, canvas/src/components/TemplateCustomizer.tsx, canvas/src/hooks/useFileWatcher.ts</files>
  <action>
    Rewrite `canvas/src/App.tsx` to use the new architecture:
    1. Replace the current flat session UI with AppShell as the root layout
    2. Main content area renders based on campaign store's currentView:
       - 'dashboard': CampaignDashboard
       - 'campaign': DrillDownGrid showing assets
       - 'asset': DrillDownGrid showing frames
       - 'frame': DrillDownGrid showing iterations (this is where annotations, status badges, timeline from existing components can be rendered per-iteration)
    3. Left sidebar: PromptSidebar (existing, with minor updates for campaign context -- pass campaignId/assetId so generation targets the right campaign)
    4. Right sidebar: ContentEditor (Teammate C) -- opens when an iteration is selected
    5. Breadcrumb in header slot
    6. When user selects an iteration in the grid: call editor store's selectIteration(id) which opens the right sidebar and loads the slot schema
    7. Template flow: "+ New" button in CampaignDashboard opens template gallery modal. After template selection + customization, creates a new asset in the campaign via API.

    Update `canvas/src/components/TemplateGallery.tsx`:
    - Now appears inside the creation flow (modal or inline panel), not as a top-level section
    - Uses template-configs.ts metadata for template list (instead of fetching from /api/templates)
    - After template selection, navigates to TemplateCustomizer then creates asset+frame+iteration via API

    Update `canvas/src/components/TemplateCustomizer.tsx`:
    - After customization, POSTs to create a new asset with source='template', including the template's SlotSchema

    Update `canvas/src/hooks/useFileWatcher.ts`:
    - Adapt file watcher to refresh campaign data on file changes (refetch current view's data)
    - Watch `.fluid/campaigns/` directory instead of (or in addition to) `.fluid/working/`
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS/canvas && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>App.tsx renders AppShell with campaign navigation. Template creation flow works through modal. Iteration selection opens right sidebar editor. File watcher tracks campaign changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify integrated application end-to-end</name>
  <action>Automated work completed in Task 1. This checkpoint verifies the full integration visually.</action>
  <what-built>Complete integrated application: campaign dashboard with drill-down navigation, template creation flow, right sidebar content editor, collapsible sidebars, breadcrumb navigation</what-built>
  <how-to-verify>
    1. Start the dev server: `cd canvas && npm run dev`
    2. Open http://localhost:5174 in browser
    3. Verify: Dashboard shows campaign list (empty initially is OK)
    4. Click "+ New Campaign" -- should see title + channel selection
    5. Create a campaign -- should appear in dashboard
    6. Click "+ New" on a campaign -- template gallery should appear (not a top-level section)
    7. Select a template -- should create an asset with iteration
    8. Drill down: campaign -> asset -> frame -> iteration -- breadcrumbs update at each level
    9. Click an iteration -- right sidebar opens with content editor fields
    10. Edit a text field -- change should appear in the iframe preview
    11. Collapse left sidebar -- should minimize to icon strip
    12. Collapse right sidebar -- should minimize
    13. Verify header typography matches Jonathan's polished style
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd canvas && npx tsc --noEmit` -- no type errors
- `cd canvas && npx vitest run` -- all tests pass
- App loads at localhost:5174 without console errors
- Full drill-down navigation works
- Template creation flow is inside "New" creation, not top-level
- Right sidebar opens on iteration selection
- Content editing updates iframe via postMessage
</verification>

<success_criteria>
Working end-to-end application: campaign dashboard, drill-down navigation at all levels, template creation inside creation flow, content editor in right sidebar, collapsible sidebars, breadcrumb navigation. User-verified via checkpoint.
</success_criteria>

<output>
After completion, create `.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-06-SUMMARY.md`
</output>
