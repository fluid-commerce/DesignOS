---
phase: 07-merge-jonathan-s-codebase-into-fluid-designos
plan: 07
type: execute
wave: 3
depends_on: ["07-06"]
files_modified:
  - .agents/skills/fluid-campaign/SKILL.md
  - canvas/src/components/DAMPicker.tsx
  - canvas/src/components/ContentEditor.tsx
  - canvas/src/components/CampaignDashboard.tsx
autonomous: true
requirements:
  - MRGR-15
  - MRGR-16
  - MRGR-17
must_haves:
  truths:
    - "/fluid-campaign skill takes a brief and decomposes into per-channel generation tasks"
    - "Campaign orchestrator dispatches to existing fluid-social, fluid-one-pager pipelines"
    - "DAM integration UI elements are merged (Browse Assets button, Fluid DAM indicator)"
    - "5 fixed option slots per channel match Jonathan's UI design"
  artifacts:
    - path: ".agents/skills/fluid-campaign/SKILL.md"
      provides: "Campaign orchestrator skill definition"
      min_lines: 50
    - path: "canvas/src/components/DAMPicker.tsx"
      provides: "DAM integration UI scaffold (Browse Assets, file attachment)"
  key_links:
    - from: ".agents/skills/fluid-campaign/SKILL.md"
      to: "fluid-social skill"
      via: "dispatches per-channel generation to existing skills"
      pattern: "fluid-social|fluid-one-pager"
    - from: "canvas/src/components/DAMPicker.tsx"
      to: "canvas/src/components/ContentEditor.tsx"
      via: "DAMPicker renders inside ContentEditor for image fields"
      pattern: "DAMPicker"
---

<objective>
Wave 3: Campaign orchestrator skill, DAM integration, and 5-slot channel UI.

Purpose: Completes the campaign generation workflow. The /fluid-campaign skill takes a marketing brief, decomposes it into per-channel generation tasks (Instagram, LinkedIn, Blog, One Pager), and dispatches to existing skill pipelines. DAM integration UI elements from Jonathan's codebase are merged. The campaign creation UI shows 5 fixed option slots per channel matching Jonathan's design.

Output: Working campaign orchestrator skill, DAM UI scaffold, 5-slot channel display.

Team Role: Single-agent execution (Wave 3 -- post-integration).
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-CONTEXT.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-RESEARCH.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-06-SUMMARY.md

@.agents/skills/fluid-social/SKILL.md
@canvas/src/components/ContentEditor.tsx
@canvas/src/components/CampaignDashboard.tsx
@Reference/Context/Jonathan's Codebase/editor.js
@Reference/Context/Jonathan's Codebase/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campaign orchestrator skill (/fluid-campaign)</name>
  <files>.agents/skills/fluid-campaign/SKILL.md</files>
  <action>
    Create `.agents/skills/fluid-campaign/SKILL.md` as a new top-level slash command skill.

    The skill definition should:
    1. Accept a marketing brief (topic, product, campaign goal, target audience)
    2. Accept channel selection: list of channels to generate for (instagram, linkedin, blog, one-pager). Default: all.
    3. Decompose the brief into per-channel generation tasks:
       - For each selected channel, craft a channel-specific prompt from the brief
       - Instagram: focus on visual impact, 1080x1080 constraint
       - LinkedIn: professional tone, landscape format
       - Blog: longer-form copy focus
       - One Pager: sales collateral with stats and comparison
    4. Create a campaign record via API: POST /api/campaigns with { title: brief summary, channels: selected channels }
    5. For each channel, create an asset record: POST /api/campaigns/:id/assets
    6. Dispatch to existing skill pipelines:
       - Instagram/LinkedIn -> delegate to fluid-social with channel-specific prompt and campaignId/assetId context
       - One Pager -> delegate to fluid-one-pager with campaignId/assetId context
       - Blog -> placeholder (no skill yet, note as future)
    7. Each dispatched skill generates up to 5 variations per channel (matching 5 fixed slots per locked decision)
    8. Pass campaignId and assetId to child skills so MCP push_asset registers output in the correct campaign hierarchy
    9. After all channels complete: summarize what was generated, link to canvas URL

    The skill follows the established orchestrator pattern from fluid-social -- spawns subagents with focused context, uses the campaign API for registration.

    Note: The brief-to-prompt bridge (how the brief becomes channel-specific prompts) is Claude's discretion per CONTEXT.md.
  </action>
  <verify>
    <automated>test -f /Users/cheyrasmussen/Fluid-DesignOS/.agents/skills/fluid-campaign/SKILL.md && echo "SKILL.md exists" && wc -l /Users/cheyrasmussen/Fluid-DesignOS/.agents/skills/fluid-campaign/SKILL.md</automated>
  </verify>
  <done>/fluid-campaign skill defined with brief decomposition, channel selection, per-channel dispatch to existing skill pipelines, and campaign API integration.</done>
</task>

<task type="auto">
  <name>Task 2: DAM integration UI scaffold and 5-slot channel display</name>
  <files>canvas/src/components/DAMPicker.tsx, canvas/src/components/ContentEditor.tsx, canvas/src/components/CampaignDashboard.tsx</files>
  <action>
    Create `canvas/src/components/DAMPicker.tsx`:
    - Port Jonathan's DAM UI elements faithfully:
      - "Fluid DAM" indicator badge (visual tag showing asset is from DAM)
      - "Browse Assets" button that will eventually open DAM picker
      - File attachment flow UI (drag-drop zone + file input)
    - Check if @fluid-commerce/dam-picker is available: `npm info @fluid-commerce/dam-picker 2>/dev/null`
      - If available: install and integrate
      - If not available (most likely): scaffold the UI as a working placeholder. Browse Assets button shows a "Connect to Fluid DAM" message. File attachment via local file input still works (uses FileReader to load image, sends to iframe via postMessage).
    - The component renders inside ContentEditor.tsx for ImageField slots (replacing the basic "Browse" placeholder from Teammate C)

    Update `canvas/src/components/ContentEditor.tsx`:
    - Replace the placeholder image browse button with DAMPicker component for ImageField slots
    - Ensure DAMPicker integrates with the postMessage pattern for updating images in iframe

    Update `canvas/src/components/CampaignDashboard.tsx`:
    - Campaign creation modal: after creating a campaign, show channel tabs with 5 fixed option slots per channel
    - Each channel tab (Instagram, LinkedIn, Blog, One Pager) displays a grid of 5 slots
    - Filled slots show the generated asset preview (iframe). Empty slots show a dashed border placeholder
    - This matches Jonathan's UI design for 5 fixed option slots per channel
    - Add a "Generate Campaign" action that invokes /fluid-campaign via the prompt sidebar or MCP
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS/canvas && npx tsc --noEmit src/components/DAMPicker.tsx src/components/ContentEditor.tsx src/components/CampaignDashboard.tsx 2>&1 | head -20</automated>
  </verify>
  <done>DAM integration UI merged with Browse Assets button and file attachment flow. 5-slot channel display in campaign view. ContentEditor uses DAMPicker for image fields.</done>
</task>

</tasks>

<verification>
- `cd canvas && npx tsc --noEmit` -- no type errors
- /fluid-campaign SKILL.md exists with complete brief decomposition logic
- DAMPicker component renders in ContentEditor for image fields
- Campaign view shows 5 fixed slots per channel
- Campaign creation flow connected to generation
</verification>

<success_criteria>
Campaign orchestrator skill takes a brief and generates across channels. DAM integration UI elements preserved from Jonathan's system. 5 fixed option slots per channel visible in campaign view. One prompt can generate an entire multi-channel campaign.
</success_criteria>

<output>
After completion, create `.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-07-SUMMARY.md`
</output>
