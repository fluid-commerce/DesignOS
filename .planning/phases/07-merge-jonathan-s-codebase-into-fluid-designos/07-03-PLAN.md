---
phase: 07-merge-jonathan-s-codebase-into-fluid-designos
plan: 03
type: execute
wave: 1
depends_on: ["07-01"]
files_modified:
  - canvas/src/store/campaign.ts
  - canvas/src/components/Breadcrumb.tsx
  - canvas/src/components/DrillDownGrid.tsx
  - canvas/src/components/CampaignDashboard.tsx
  - canvas/src/components/AppShell.tsx
autonomous: true
requirements:
  - MRGR-05
  - MRGR-06
  - MRGR-07
must_haves:
  truths:
    - "User sees a unified dashboard with campaigns as the primary organizing unit"
    - "User can drill down from Campaign to Asset to Frame to Iteration using full-size previews"
    - "Breadcrumb navigation shows current position and allows jumping to any level"
    - "Both sidebars can collapse to icons independently"
  artifacts:
    - path: "canvas/src/store/campaign.ts"
      provides: "Zustand store for campaign navigation state and data fetching"
      exports: ["useCampaignStore"]
    - path: "canvas/src/components/Breadcrumb.tsx"
      provides: "Breadcrumb navigation for Campaign / Asset / Frame / Iteration"
    - path: "canvas/src/components/DrillDownGrid.tsx"
      provides: "Reusable grid component for all drill-down levels using iframe previews"
    - path: "canvas/src/components/CampaignDashboard.tsx"
      provides: "Top-level campaign list view with filter/sort"
    - path: "canvas/src/components/AppShell.tsx"
      provides: "Three-panel layout with collapsible sidebars"
  key_links:
    - from: "canvas/src/store/campaign.ts"
      to: "/api/campaigns"
      via: "fetch calls to REST API"
      pattern: "fetch.*api/campaigns"
    - from: "canvas/src/components/DrillDownGrid.tsx"
      to: "canvas/src/components/AssetFrame.tsx"
      via: "renders iframes at native dimensions"
      pattern: "AssetFrame"
---

<objective>
Teammate B: Navigation UI -- dashboard, drill-down grid, breadcrumbs, and app shell with collapsible sidebars.

Purpose: Replaces the flat session sidebar with a campaign-centric navigation system. Users see campaigns as the primary unit, drill down through assets/frames/iterations using full-size iframe previews (reusing VariationGrid pattern), and navigate via breadcrumbs. The app shell provides a three-panel layout with independently collapsible sidebars.

Output: Campaign dashboard, drill-down navigation at all 4 levels, breadcrumb bar, and the three-panel app shell layout.

Team Role: **Teammate B (Navigation & App Shell)**
- Owns exclusively: campaign.ts store, Breadcrumb.tsx, DrillDownGrid.tsx, CampaignDashboard.tsx, AppShell.tsx
- Consumes from Wave 0: campaign-types.ts interfaces
- Consumes from Teammate A: /api/campaigns/* endpoints (via fetch)
- Produces for integration: AppShell layout that Teammate C's ContentEditor slots into (right sidebar), existing PromptSidebar slots into (left sidebar)
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-CONTEXT.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-RESEARCH.md
@.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-01-SUMMARY.md

@canvas/src/components/VariationGrid.tsx
@canvas/src/components/AssetFrame.tsx
@canvas/src/components/SessionSidebar.tsx
@canvas/src/App.tsx
@canvas/src/lib/campaign-types.ts

<interfaces>
From canvas/src/lib/campaign-types.ts (from Wave 0):
```typescript
export interface Campaign { id: string; title: string; channels: string[]; createdAt: number; updatedAt: number; }
export interface Asset { id: string; campaignId: string; title: string; assetType: string; frameCount: number; createdAt: number; }
export interface Frame { id: string; assetId: string; frameIndex: number; createdAt: number; }
export interface Iteration { id: string; frameId: string; iterationIndex: number; htmlPath: string; slotSchema: object | null; aiBaseline: object | null; userState: object | null; status: VariationStatus; source: 'ai' | 'template'; templateId: string | null; createdAt: number; }
```

From canvas/src/components/VariationGrid.tsx (existing, reuse pattern):
```typescript
// Renders AssetFrame components in a responsive grid
// Uses iframes at native dimensions -- this same pattern is used at every drill-down level
```

API endpoints (from Teammate A):
- GET /api/campaigns -> Campaign[]
- GET /api/campaigns/:id/assets -> Asset[]
- GET /api/assets/:id/frames -> Frame[]
- GET /api/frames/:id/iterations -> Iteration[]
- GET /api/iterations/:id -> Iteration with html content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Campaign Zustand store and AppShell layout</name>
  <files>canvas/src/store/campaign.ts, canvas/src/components/AppShell.tsx</files>
  <action>
    Create `canvas/src/store/campaign.ts` (Zustand store) managing:
    - Navigation state: currentView ('dashboard' | 'campaign' | 'asset' | 'frame'), activeCampaignId, activeAssetId, activeFrameId, activeIterationId
    - Data cache: campaigns (Campaign[]), assets (Asset[]), frames (Frame[]), iterations (Iteration[])
    - Navigation actions: navigateToCampaign(id), navigateToAsset(id), navigateToFrame(id), selectIteration(id), navigateBack(), navigateToDashboard()
    - Data fetching actions: fetchCampaigns(), fetchAssets(campaignId), fetchFrames(assetId), fetchIterations(frameId)
    - Each navigate action fetches the children for that level (e.g., navigateToCampaign fetches assets)
    - Sidebar state: leftSidebarOpen (default true), rightSidebarOpen (default false), toggleLeftSidebar(), toggleRightSidebar(), setRightSidebarOpen(boolean)
    - Follow the existing sessions.ts store pattern for race condition guards

    Create `canvas/src/components/AppShell.tsx`:
    - Three-panel layout: left sidebar (collapsible), main content area, right sidebar (collapsible)
    - Left sidebar: renders children passed as `leftSidebar` prop (will be PromptSidebar). Default: open. Collapsed: shows icon strip (chat icon)
    - Right sidebar: renders children passed as `rightSidebar` prop (will be ContentEditor from Teammate C). Default: closed. Opens when an iteration is selected.
    - Main content: renders children passed as `children` prop (CampaignDashboard or DrillDownGrid based on currentView)
    - Header area with breadcrumbs (slot for Breadcrumb component)
    - Header mirrors Jonathan's visual design: clean typography, generous spacing, polished badge style. Reference Jonathan's index.html header for visual cues.
    - Collapsible sidebars use CSS transitions (width 0 with overflow hidden, or translate). Claude's discretion on animation details.
    - No brand reference bar (per locked decision).
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS/canvas && npx tsc --noEmit src/store/campaign.ts src/components/AppShell.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Campaign store manages navigation state with drill-down actions. AppShell renders three-panel layout with collapsible sidebars.</done>
</task>

<task type="auto">
  <name>Task 2: Breadcrumb, DrillDownGrid, and CampaignDashboard</name>
  <files>canvas/src/components/Breadcrumb.tsx, canvas/src/components/DrillDownGrid.tsx, canvas/src/components/CampaignDashboard.tsx</files>
  <action>
    Create `canvas/src/components/Breadcrumb.tsx`:
    - Renders current navigation path: Dashboard / Campaign Title / Asset Title / Frame N / vN
    - Each segment is clickable, navigating to that level via campaign store actions
    - Back button (left arrow icon) for one-level-up navigation
    - Shows only relevant segments (dashboard view shows nothing or just "Campaigns")
    - Clean typography matching Jonathan's header style

    Create `canvas/src/components/DrillDownGrid.tsx`:
    - Generic grid component parameterized by item type
    - Props: items, renderPreview (returns html string + dimensions), onSelect, onAction (optional context menu), emptyState, title
    - Renders items in a responsive grid using the existing VariationGrid iframe pattern
    - Full-size previews (iframes at native dimensions) -- NOT thumbnail grids (per locked decision)
    - At campaign level: shows one representative asset per campaign (first asset's first iteration)
    - At asset level: shows first frame's latest iteration for each asset
    - At frame level: shows latest iteration per frame
    - At iteration level: shows all iterations for the selected frame
    - Each item has a title overlay and click handler for drill-down
    - Handles empty state gracefully (e.g., "No campaigns yet. Create one to get started.")

    Create `canvas/src/components/CampaignDashboard.tsx`:
    - Top-level view showing all campaigns
    - Uses DrillDownGrid with campaigns as items
    - "+ New Campaign" button in header area (triggers creation flow -- initially just a simple title + channel selection modal)
    - Filter/sort controls: filter by content type (channel), sort by date (per locked decision: single view with filter/sort, not separate tabs)
    - Uses campaign store for data and navigation
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS/canvas && npx tsc --noEmit src/components/Breadcrumb.tsx src/components/DrillDownGrid.tsx src/components/CampaignDashboard.tsx 2>&1 | head -20</automated>
  </verify>
  <done>Breadcrumb shows navigation path with clickable segments. DrillDownGrid renders full-size iframe previews at every level. CampaignDashboard shows campaign list with filter/sort.</done>
</task>

</tasks>

<verification>
- `cd canvas && npx tsc --noEmit` -- no type errors
- Campaign store navigates between all 4 levels
- DrillDownGrid renders iframes at native dimensions (not thumbnails)
- Breadcrumb updates at each navigation level
- AppShell sidebars collapse/expand independently
- No brand reference bar in the UI
</verification>

<success_criteria>
User can see campaigns in a dashboard, drill down through assets/frames/iterations with full-size previews, navigate via breadcrumbs, and control sidebar visibility. The app shell layout has slots for left sidebar (AI chat) and right sidebar (content editor).
</success_criteria>

<output>
After completion, create `.planning/phases/07-merge-jonathan-s-codebase-into-fluid-designos/07-03-SUMMARY.md`
</output>
