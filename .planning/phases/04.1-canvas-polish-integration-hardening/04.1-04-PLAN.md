---
phase: 04.1-canvas-polish-integration-hardening
plan: 04
type: execute
wave: 3
depends_on: ["04.1-01", "04.1-02", "04.1-03"]
files_modified:
  - canvas/src/components/PromptSidebar.tsx
  - canvas/src/components/StreamMessage.tsx
  - canvas/src/components/TemplateGallery.tsx
  - canvas/src/components/TemplateCustomizer.tsx
  - canvas/src/lib/templates.ts
  - canvas/src/server/watcher.ts
  - canvas/src/App.tsx
  - canvas/src/__tests__/template-gallery.test.tsx
  - canvas/src/__tests__/template-endpoint.test.ts
  - canvas/src/__tests__/prompt-sidebar.test.tsx
autonomous: false
requirements: [POL-07]

must_haves:
  truths:
    - "Template gallery shows available templates as live HTML previews in iframes"
    - "User can pick a template and enter customization (headline, accent color, topic)"
    - "Customization form triggers generation via the /api/generate endpoint"
    - "Left sidebar shows agent output stream as chat-like messages during generation"
    - "Two entry points work: Create from template and Create with AI (free prompt)"
    - "Canvas layout has left sidebar (prompt/stream) + main pane (gallery or session)"
    - "When generation completes, canvas auto-switches to session view"
  artifacts:
    - path: "canvas/src/components/PromptSidebar.tsx"
      provides: "Left sidebar with prompt input and stream display"
      contains: "useGenerationStream"
    - path: "canvas/src/components/StreamMessage.tsx"
      provides: "Renders individual stream events as chat bubbles"
    - path: "canvas/src/components/TemplateGallery.tsx"
      provides: "Grid of template cards with live iframe previews"
      contains: "srcDoc"
    - path: "canvas/src/components/TemplateCustomizer.tsx"
      provides: "Customization form (headline, accent color, topic) before generation"
    - path: "canvas/src/lib/templates.ts"
      provides: "Template type definitions and helpers"
      exports: ["TemplateInfo"]
    - path: "canvas/src/server/watcher.ts"
      provides: "/api/templates endpoint + /fluid-assets/ static serving"
      contains: "api/templates"
    - path: "canvas/src/App.tsx"
      provides: "New layout: left sidebar + main pane with view routing"
  key_links:
    - from: "canvas/src/components/TemplateGallery.tsx"
      to: "/api/templates"
      via: "fetch GET"
      pattern: "fetch.*api/templates"
    - from: "canvas/src/components/TemplateCustomizer.tsx"
      to: "canvas/src/components/PromptSidebar.tsx"
      via: "triggers generation via useGenerationStream"
      pattern: "generate"
    - from: "canvas/src/components/PromptSidebar.tsx"
      to: "canvas/src/hooks/useGenerationStream.ts"
      via: "hook consumption"
      pattern: "useGenerationStream"
    - from: "canvas/src/App.tsx"
      to: "canvas/src/components/PromptSidebar.tsx"
      via: "layout composition"
      pattern: "PromptSidebar"
    - from: "canvas/src/store/generation.ts"
      to: "canvas/src/App.tsx"
      via: "generation status === complete triggers mainView switch to session"
      pattern: "status.*complete.*setMainView.*session"
---

<objective>
Build the canvas control plane UI: template gallery with live previews, template customization form, prompt sidebar with streaming agent output display, and rewire App.tsx layout for the new sidebar + main pane architecture.

Purpose: Transforms the canvas from a passive viewer into a self-contained creative workspace where users browse templates, customize, generate, and see agent output -- all in one UI.
Output: PromptSidebar, StreamMessage, TemplateGallery, TemplateCustomizer components, /api/templates endpoint, App.tsx layout overhaul.
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-canvas-polish-integration-hardening/04.1-CONTEXT.md
@.planning/phases/04.1-canvas-polish-integration-hardening/04.1-RESEARCH.md
@.planning/phases/04.1-canvas-polish-integration-hardening/04.1-01-SUMMARY.md
@.planning/phases/04.1-canvas-polish-integration-hardening/04.1-03-SUMMARY.md

<interfaces>
<!-- Key types from prior plans -->

From canvas/src/lib/stream-parser.ts (created in Plan 03):
```typescript
export interface StreamUIMessage {
  id: string;
  type: 'text' | 'tool-start' | 'tool-done' | 'status' | 'error';
  content: string;
  toolName?: string;
  timestamp: number;
}
export function parseStreamEvent(event: any, sseEventType?: string): StreamUIMessage | null;
```

From canvas/src/store/generation.ts (created in Plan 03):
```typescript
export const useGenerationStore: () => {
  status: 'idle' | 'generating' | 'complete' | 'error';
  events: StreamUIMessage[];
  activeSessionId: string | null;
  // ...
}
```

From canvas/src/hooks/useGenerationStream.ts (created in Plan 03):
```typescript
export function useGenerationStream(): {
  generate: (prompt: string, opts?: { template?: string; customization?: object; skillType?: string }) => Promise<void>;
  status: 'idle' | 'generating' | 'complete' | 'error';
  events: StreamUIMessage[];
}
```

From canvas/src/components/AssetFrame.tsx (existing, updated in Plan 01):
```typescript
// iframe srcDoc pattern for rendering HTML at native dimensions:
<iframe
  srcDoc={html}
  sandbox="allow-same-origin"
  style={{
    width: dims.width, height: dims.height,
    border: 'none',
    transform: `scale(${scale})`,
    transformOrigin: 'top left',
  }}
/>
```

From RESEARCH.md (template discovery):
```typescript
interface TemplateInfo {
  id: string;
  name: string;
  category: 'social' | 'one-pager';
  html: string;
  dimensions: { width: number; height: number };
}
```

From RESEARCH.md (customization):
```typescript
interface TemplateCustomization {
  headline: string;
  accentColor: 'orange' | 'blue' | 'green' | 'purple';
  topic: string;
  platform?: string;
  variations: number;  // default: 3
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Template types + API endpoint + static asset serving</name>
  <files>canvas/src/lib/templates.ts, canvas/src/server/watcher.ts, canvas/src/__tests__/template-endpoint.test.ts</files>
  <behavior>
    - Test: GET /api/templates returns JSON array of TemplateInfo objects
    - Test: social templates have dimensions {1080, 1080}, one-pager templates have {816, 1056}
    - Test: index.html files are excluded from template listing
    - Test: template HTML has asset paths rewritten from ../../assets/ to /fluid-assets/
    - Test: GET /fluid-assets/logo.svg serves file from project assets/ directory with correct content-type
  </behavior>
  <action>
1. Create `canvas/src/lib/templates.ts` -- type definitions:
   ```typescript
   export interface TemplateInfo {
     id: string;
     name: string;
     category: 'social' | 'one-pager';
     html: string;
     dimensions: { width: number; height: number };
   }

   export interface TemplateCustomization {
     headline: string;
     accentColor: 'orange' | 'blue' | 'green' | 'purple';
     topic: string;
     platform?: string;
     variations: number;
   }
   ```

2. Create `canvas/src/__tests__/template-endpoint.test.ts`:
   - Mock fs.readdir and fs.readFile to simulate template files
   - Test the /api/templates handler returns correct TemplateInfo shape
   - Test index.html exclusion
   - Test asset path rewriting in HTML content
   - Test /fluid-assets/ static serving with correct MIME types

3. Add to `canvas/src/server/watcher.ts`:
   - `GET /api/templates` endpoint: reads `templates/social/*.html` and `templates/one-pagers/*.html` from project root. Returns JSON array of TemplateInfo objects. Skip `index.html` files. Social templates get dimensions {1080, 1080}, one-pager gets {816, 1056}.
   - Static asset serving: add a middleware route for `/fluid-assets/` that serves files from the project's `assets/` directory. Use `fs.readFile` and set appropriate content-type headers based on file extension (.svg -> image/svg+xml, .png -> image/png, .jpg -> image/jpeg, .woff2 -> font/woff2).
   - Before returning template HTML in the API response, rewrite relative asset paths (`../../assets/`) to absolute `/fluid-assets/` paths using string replace. This ensures iframe srcDoc previews render images correctly.
  </action>
  <verify>
    <automated>cd canvas && npx vitest run src/__tests__/template-endpoint.test.ts</automated>
  </verify>
  <done>Template types defined. /api/templates endpoint returns template data with rewritten asset paths. /fluid-assets/ serves static files. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Template gallery + customizer components</name>
  <files>canvas/src/components/TemplateGallery.tsx, canvas/src/components/TemplateCustomizer.tsx, canvas/src/__tests__/template-gallery.test.tsx</files>
  <behavior>
    - Test: gallery fetches templates on mount and renders a card for each template
    - Test: clicking a template card calls onSelectTemplate with the template
    - Test: "Create with AI" card is rendered and calls onFreePrompt when clicked
    - Test: customizer renders form fields (headline, accent color, topic, variations)
    - Test: customizer "Generate" button calls generate with constructed prompt and customization
    - Test: customizer "Back to Templates" calls onBack
  </behavior>
  <action>
1. Create `canvas/src/__tests__/template-gallery.test.tsx`:
   - Mock fetch to return sample template data
   - Test gallery renders template cards with iframe previews
   - Test click handlers fire correctly
   - Test customizer form fields render and submit correctly

2. Create `canvas/src/components/TemplateGallery.tsx`:
   - Fetches templates from `/api/templates` on mount
   - Renders a grid of template cards (3 columns on wide screens, 2 on medium, 1 on narrow)
   - Each card: scaled-down iframe preview (~280px wide) + template name below + category badge
   - Iframe uses srcDoc with sandbox="allow-same-origin" and pointerEvents: 'none' (prevent interaction)
   - Scale calculation: `280 / template.dimensions.width`
   - Click on card calls `onSelectTemplate(template)` prop
   - Include "Create with AI" card at the top (no template, just opens prompt input via `onFreePrompt` prop)
   - Dark theme matching existing canvas aesthetic (#1a1a2e background, #e0e0e0 text, #2a2a3e borders)

3. Create `canvas/src/components/TemplateCustomizer.tsx`:
   - Shown when user selects a template from gallery
   - Displays selected template preview (larger, ~400px) on the left
   - Form on the right with fields: headline (text input), accent color (4 color buttons: orange/blue/green/purple with hex swatches), topic/brief (textarea), platform (dropdown for social: instagram/linkedin, hidden for one-pager), number of variations (1-5, default 3)
   - "Generate" button triggers generation: constructs prompt from customization fields + template context, calls the generate function from useGenerationStream
   - "Back to Templates" link to return to gallery via `onBack` prop
   - Form uses same dark theme styling as existing canvas
  </action>
  <verify>
    <automated>cd canvas && npx vitest run src/__tests__/template-gallery.test.tsx</automated>
  </verify>
  <done>Template gallery renders live HTML preview cards. Customizer form with all fields triggers generation. Tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prompt sidebar + stream display + App layout overhaul</name>
  <files>canvas/src/components/PromptSidebar.tsx, canvas/src/components/StreamMessage.tsx, canvas/src/App.tsx, canvas/src/__tests__/prompt-sidebar.test.tsx</files>
  <behavior>
    - Test: PromptSidebar renders textarea and Generate button
    - Test: PromptSidebar textarea is disabled when generation status is 'generating'
    - Test: StreamMessage renders text type as dark bubble, tool-start as status pill, error as red bubble
    - Test: StreamMessage renders status type as centered grey text
    - Test: App renders gallery view by default when no session is active
    - Test: App routes between gallery, customizer, and session views based on mainView state
    - Test: when generation status transitions to 'complete', mainView switches to 'session'
  </behavior>
  <action>
1. Create `canvas/src/__tests__/prompt-sidebar.test.tsx`:
   - Test PromptSidebar renders prompt input area and generate button
   - Test textarea disabled state during generation
   - Test StreamMessage renders each message type with correct styling class/element
   - Test App layout: PromptSidebar always present, main pane shows correct view
   - Test auto-switch: mock useGenerationStore to return status='complete', verify mainView transitions to 'session'

2. Create `canvas/src/components/StreamMessage.tsx`:
   - Renders a single StreamUIMessage as a chat-like bubble
   - Text type: dark bubble (#2a2a3e background) with white text, left-aligned (agent message)
   - Tool-start type: small status pill with tool icon indicator (e.g., "Reading file..." "Writing file...")
   - Status type: centered grey text (system message)
   - Error type: red-tinted bubble (#3e2a2a background)
   - Compact spacing between messages (4px gap)
   - Accumulation strategy: The PromptSidebar component handles accumulating consecutive text messages into a single expanding bubble -- StreamMessage renders whatever content it receives

3. Create `canvas/src/components/PromptSidebar.tsx`:
   - Fixed-width left sidebar (~320px)
   - Top section: prompt input
     - "Create with AI" mode: textarea + "Generate" button
     - When generating: input disabled, button shows "Generating..."
   - Main section: scrollable stream display area
     - Maps over generation events from useGenerationStream
     - Accumulates consecutive 'text' events into a single expanding message (using a ref to track whether last event was text -- if so, update last message content instead of adding new)
     - Auto-scrolls to bottom as new events arrive (useEffect with ref)
     - Shows generation status indicator (idle: empty state with prompt, generating: streaming messages, complete: "Done" indicator)
   - Uses useGenerationStream hook for state and generate function
   - When user submits a free prompt (no template), calls `generate(prompt, { skillType: 'social' })` (default to social, can be changed)
   - View-only during generation per user decision -- textarea disabled while status==='generating'
   - Always present regardless of which view is showing in main pane

4. Overhaul `canvas/src/App.tsx` layout:
   - New layout structure: `PromptSidebar` (left, always visible) | Main Pane | Right sidebar (timeline/notes, only when session active)
   - Main pane has view routing:
     - `'gallery'` view: TemplateGallery (default when no session active)
     - `'customizer'` view: TemplateCustomizer (after picking a template)
     - `'session'` view: VariationGrid + IteratePanel (when a session is active)
   - View state management: `const [mainView, setMainView] = useState<'gallery' | 'customizer' | 'session'>('gallery')`
   - **Auto-switch on generation complete:** Add a useEffect watching `generationStatus` from useGenerationStore. When status transitions to `'complete'`, call `setMainView('session')` and refresh sessions to pick up the new one. This is the critical link between generation completion and session viewing.
   - SessionSidebar moves: integrate session list into the left PromptSidebar as a collapsible section below the prompt, or keep as a secondary element. Since prompt sidebar replaces the session sidebar's position, merge session list into the bottom of PromptSidebar as a "Recent Sessions" list (simpler, avoids two left sidebars).
   - Header stays at top spanning full width
   - The existing SessionSidebar component can be imported into PromptSidebar as a sub-component
  </action>
  <verify>
    <automated>cd canvas && npx vitest run src/__tests__/prompt-sidebar.test.tsx</automated>
  </verify>
  <done>Left sidebar shows prompt input and streaming agent output. App layout supports gallery/customizer/session views. Generation complete triggers auto-switch to session view. All tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual and functional verification of complete canvas control plane</name>
  <files>canvas/src/App.tsx</files>
  <action>
Human verification of the complete canvas control plane built across all 4 plans. Start the canvas and verify:
1. Start the canvas: `cd canvas && npm run dev`
2. Verify the new layout: left sidebar (prompt area + sessions), main pane shows template gallery
3. Click a template card -- verify live HTML preview renders correctly in the gallery
4. Click a template -- verify customization form appears (headline, accent color, topic fields)
5. Type a free prompt in the sidebar and click Generate -- verify agent stream appears as chat messages (requires claude CLI in PATH)
6. Open an existing session -- verify star icon on variation frames (not the old status cycle)
7. Click star on a variation -- verify it toggles winner without auto-rejecting others
8. With a single variation, verify textarea is enabled and Iterate button works without starring
9. With multiple variations, verify Iterate button requires a starred winner
10. Create a new file in `.fluid/working/test-session/` and verify it appears in canvas without refresh
  </action>
  <verify>Human confirms all 10 verification steps pass. Type "approved" or describe issues.</verify>
  <done>Canvas control plane is visually correct and functionally complete. All UX flows work as specified.</done>
</task>

</tasks>

<verification>
```bash
cd canvas && npx vitest run src/__tests__/template-endpoint.test.ts src/__tests__/template-gallery.test.tsx src/__tests__/prompt-sidebar.test.tsx
```
</verification>

<success_criteria>
- Template gallery renders live HTML previews using iframe srcDoc pattern
- Template customization form feeds into CLI generation
- Prompt sidebar shows real-time agent stream as chat messages
- App layout: left sidebar + main pane (gallery/customizer/session) + right sidebar (timeline/notes)
- Two entry points work: "Create from template" and "Create with AI"
- Generation complete auto-switches to session view
- All existing and new tests pass
- Human verification confirms visual/functional quality
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-canvas-polish-integration-hardening/04.1-04-SUMMARY.md`
</output>
