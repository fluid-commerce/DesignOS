---
phase: 14-design-dna
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - canvas/src/lib/db.ts
  - canvas/src/server/db-api.ts
  - canvas/src/server/brand-seeder.ts
  - canvas/src/server/watcher.ts
autonomous: true
requirements:
  - DNA-01
  - DNA-02
  - DNA-03
  - DNA-04

must_haves:
  truths:
    - "template_design_rules table exists in SQLite with scope, platform, archetype_slug, label, content, and sort_order columns"
    - "Global visual style rules are seeded into brand_patterns table under category 'visual-style'"
    - "Per-deliverable design rules (social general, Instagram-specific, LinkedIn-specific) are seeded into template_design_rules"
    - "Per-archetype design notes for all 7 social templates are seeded into template_design_rules"
    - "API endpoints serve design rules data for UI consumption"
  artifacts:
    - path: "canvas/src/lib/db.ts"
      provides: "template_design_rules CREATE TABLE"
      contains: "template_design_rules"
    - path: "canvas/src/server/brand-seeder.ts"
      provides: "seedDesignRules and seedGlobalVisualStyle functions"
      exports: ["seedDesignRulesIfEmpty", "seedGlobalVisualStyleIfEmpty"]
    - path: "canvas/src/server/db-api.ts"
      provides: "CRUD for template_design_rules"
      exports: ["getDesignRules", "getDesignRule", "updateDesignRule"]
    - path: "canvas/src/server/watcher.ts"
      provides: "API routes for design rules"
  key_links:
    - from: "canvas/src/server/watcher.ts"
      to: "canvas/src/server/db-api.ts"
      via: "getDesignRules import and /api/design-rules route"
      pattern: "getDesignRules"
    - from: "canvas/src/server/watcher.ts"
      to: "canvas/src/server/brand-seeder.ts"
      via: "seedDesignRulesIfEmpty call at startup"
      pattern: "seedDesignRulesIfEmpty"
---

<objective>
Create the DB schema, seeder, and API layer for Design DNA — the layered brand intelligence system that gives generation agents access to global visual style rules and per-deliverable/per-archetype design notes.

Purpose: Agents currently generate social posts that look like "mini web pages" because they lack explicit visual style intelligence. This plan creates the data foundation: a new `template_design_rules` table for per-deliverable design DNA, global visual style rules seeded into `brand_patterns`, and API endpoints to serve this data.

Output: DB table with seeded design rules, db-api query functions, API endpoints for UI and pipeline consumption.
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-design-dna-template-extracted-style-rules-per-deliverable-design-intelligence-and-exemplar-referenced-generation-pipeline/14-CONTEXT.md

<interfaces>
From canvas/src/lib/db.ts:
```typescript
// Schema pattern: CREATE TABLE IF NOT EXISTS + try/catch ALTER TABLE migrations
export function getDb(): Database.Database;
export function closeDb(): void;
```

From canvas/src/server/db-api.ts:
```typescript
// Pattern for new tables: interface + rowToX converter + CRUD functions
export interface BrandPattern { id: string; slug: string; label: string; category: string; content: string; sortOrder: number; updatedAt: number; }
export function getBrandPatterns(category?: string): BrandPattern[];
export function getBrandPatternBySlug(slug: string): BrandPattern | undefined;
export interface VoiceGuideDoc { id: string; slug: string; label: string; content: string; sortOrder: number; updatedAt: number; }
export function getVoiceGuideDocs(): VoiceGuideDoc[];
export function updateVoiceGuideDoc(slug: string, content: string): void;
```

From canvas/src/server/brand-seeder.ts:
```typescript
export async function seedVoiceGuideIfEmpty(voiceGuideDir: string): Promise<number>;
export async function seedBrandPatternsIfEmpty(patternsHtmlPath: string): Promise<number>;
```

From canvas/src/server/watcher.ts (API pattern):
```typescript
// Routes follow pattern: if (url === '/api/X' && method === 'GET') { ... res.end(JSON.stringify(data)) }
// PUT for updates: if (url === '/api/X' && method === 'PUT') { ... body parsing ... update function ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB schema + db-api for template_design_rules</name>
  <files>canvas/src/lib/db.ts, canvas/src/server/db-api.ts</files>
  <read_first>
    - canvas/src/lib/db.ts
    - canvas/src/server/db-api.ts
  </read_first>
  <action>
1. In `canvas/src/lib/db.ts`, add a new CREATE TABLE statement inside `initSchema()` (after the brand_patterns table):

```sql
CREATE TABLE IF NOT EXISTS template_design_rules (
  id TEXT PRIMARY KEY,
  scope TEXT NOT NULL,
  platform TEXT,
  archetype_slug TEXT,
  label TEXT NOT NULL,
  content TEXT NOT NULL,
  sort_order INTEGER NOT NULL DEFAULT 0,
  updated_at INTEGER NOT NULL
);
```

The `scope` column values: 'global-social' (general social media rules), 'platform' (Instagram/LinkedIn specific), 'archetype' (per-template design notes). The `platform` column: null for global-social, 'instagram'/'linkedin' for platform rules, 'instagram'/'linkedin' for archetype notes. The `archetype_slug` column: null for global-social and platform rules, template slug like 'problem-first'/'quote'/'stat-proof' for archetype notes.

2. In `canvas/src/server/db-api.ts`, add after the Brand Patterns section:

A new interface:
```typescript
export interface DesignRule {
  id: string;
  scope: string;
  platform: string | null;
  archetypeSlug: string | null;
  label: string;
  content: string;
  sortOrder: number;
  updatedAt: number;
}
```

A `rowToDesignRule` converter function following the same pattern as `rowToBrandPattern`.

Query functions:
- `getDesignRules(scope?: string, platform?: string): DesignRule[]` — returns all rules, optionally filtered by scope and/or platform, ordered by sort_order
- `getDesignRule(id: string): DesignRule | undefined` — single rule by ID
- `getDesignRulesByArchetype(archetypeSlug: string, platform?: string): DesignRule[]` — returns archetype-specific rules, optionally filtered by platform
- `updateDesignRule(id: string, content: string): void` — updates content and updated_at timestamp

All functions follow the same pattern as existing db-api functions: `const db = getDb()`, prepared statement, return mapped results.
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS/canvas && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - canvas/src/lib/db.ts contains `CREATE TABLE IF NOT EXISTS template_design_rules`
    - canvas/src/lib/db.ts template_design_rules has columns: id, scope, platform, archetype_slug, label, content, sort_order, updated_at
    - canvas/src/server/db-api.ts contains `export interface DesignRule`
    - canvas/src/server/db-api.ts contains `export function getDesignRules`
    - canvas/src/server/db-api.ts contains `export function getDesignRule`
    - canvas/src/server/db-api.ts contains `export function getDesignRulesByArchetype`
    - canvas/src/server/db-api.ts contains `export function updateDesignRule`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>template_design_rules table defined in schema, DesignRule interface and 4 query functions exported from db-api.ts, TypeScript compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Seed global visual style rules + per-deliverable design DNA</name>
  <files>canvas/src/server/brand-seeder.ts, canvas/src/server/watcher.ts</files>
  <read_first>
    - canvas/src/server/brand-seeder.ts
    - canvas/src/server/watcher.ts
    - canvas/src/server/db-api.ts
    - templates/social/problem-first.html
    - templates/social/quote.html
    - templates/social/stat-proof.html
  </read_first>
  <action>
1. In `canvas/src/server/brand-seeder.ts`, add two new seed functions after `seedBrandPatternsIfEmpty`:

**`seedGlobalVisualStyleIfEmpty()`** — seeds one row into `brand_patterns` table with:
- slug: 'visual-compositor-contract'
- label: 'Visual Compositor Contract'
- category: 'visual-style'
- content: A markdown document containing these rules (extracted from CONTEXT.md template analysis):

```markdown
# Visual Compositor Contract — Fluid Social Posts

These rules define what makes a Fluid social post look "designed" rather than a web page.

## Canvas
- Fixed canvas with `overflow: hidden` — this is a poster, not a web page
- No scrolling. Content must fit within the fixed dimensions.

## Required Visual Layers (back to front)
1. **Background** — solid color or gradient, extends to all edges
2. **Texture/Brushstroke** — at least one decorative element with `mix-blend-mode: screen` and `opacity: 0.10–0.25`, positioned partially off-canvas (edge-bleed)
3. **Content** — headline, subtext, positioned via `position: absolute` (not flexbox/grid)
4. **Footer** — Fluid logo + We-Commerce wordmark left, Fluid dots right

## Typography Scale
- Headline must be 5–8x larger than body text (e.g., headline 72–120px, body 14–18px)
- Use only `flfontbold` for brand taglines and `NeueHaas` (Inter) for all other text
- Maximum 2 font families per post

## Positioning
- All major elements positioned with `position: absolute` and pixel values
- Content intentionally inset from edges (40–80px padding from canvas border)
- Background extends to edges; content does not

## Required Elements
- Logo (Fluid logo from brand assets)
- At least one tagline in flfontbold
- At least one decorative element (brushstroke or circle sketch)
- At least one `mix-blend-mode` or CSS `filter` effect

## Color
- Limited palette: one accent color + black (#000) + white (#fff) + opacity variants
- Accent colors: orange (#FF6B35) for pain, blue (#4A90D9) for trust, green (#7BC67E) for proof, purple (#9B59B6) for premium
- Use `rgba()` for opacity variants, not separate color definitions

## Anti-Patterns (reframed as positive constraints)
- Use `position: absolute` for layout, not CSS Grid or Flexbox
- Use pixel dimensions matching the target canvas (1080x1080 for Instagram)
- All @font-face declarations must use URLs from list_brand_assets, never base64
- All image/texture references must use URLs from list_brand_assets, never base64
```

Use `INSERT OR IGNORE` with nanoid, same idempotent COUNT(*) guard as existing seeders.

**`seedDesignRulesIfEmpty()`** — seeds the `template_design_rules` table with these rows (if COUNT(*) = 0):

Scope 'global-social' (sort_order 0):
- label: 'Social Media — General Rules'
- content: General rules about social media posts — canvas dimensions, the compositor layer model, footer requirements, typography scale ratio. Cross-reference the Visual Compositor Contract for full details. These rules apply to ALL social media posts regardless of platform.

Scope 'platform', platform 'instagram' (sort_order 10):
- label: 'Instagram Brand Guidelines'
- content: Instagram-specific rules — 1080x1080px square canvas, high-contrast text for mobile viewing, bold headline-forward compositions, touch-friendly CTA placement (bottom third).

Scope 'platform', platform 'linkedin' (sort_order 11):
- label: 'LinkedIn Brand Guidelines'
- content: LinkedIn-specific rules — 1200x627px landscape canvas, professional but bold tone, slightly more restrained brushstroke usage, text-heavy compositions acceptable (professional audience reads more).

Scope 'archetype' for each of the 7 templates (sort_order 20-26):
- problem-first (platform: 'instagram'): "Problem-First — headline dominates canvas (60%+ of visual weight). Pain-point text in large flfontbold. Accent: orange (#FF6B35). Dark background with brushstroke texture bleeding off top-right edge. Subtext small and secondary."
- quote (platform: 'instagram'): "Quote/Testimonial — centered quote in large flfontbold with circle sketch accent behind key word. Attribution smaller below. Brushstroke texture subtle, background usually dark."
- stat-proof (platform: 'instagram'): "Stat Proof — giant number (120px+) as focal point. Supporting text frames the stat's meaning. Green (#7BC67E) accent for proof/evidence. Minimal decoration — the number IS the decoration."
- app-highlight (platform: 'instagram'): "App Highlight — product feature showcase. Blue (#4A90D9) accent for trust. Screenshot or UI element as visual anchor. Text describes the feature benefit, not the feature itself."
- manifesto (platform: 'instagram'): "Manifesto — brand philosophy statement. Large flfontbold text fills most of canvas. Minimal decoration — text IS the design. Purple (#9B59B6) accent for premium/aspirational."
- partner-alert (platform: 'instagram'): "Partner Alert — partnership announcement. Dual-brand friendly layout. Blue (#4A90D9) accent. Logo placement prominent. Announcement-style headline."
- feature-spotlight (platform: 'instagram'): "Feature Spotlight — product capability deep-dive. Blue (#4A90D9) accent for trust/technology. More technical copy acceptable. Visual balance between text and illustrative element."

Each archetype row should have archetype_slug matching the template filename (without .html).

Use nanoid for IDs, Date.now() for updated_at. Same idempotent pattern: check COUNT(*) before inserting.

2. In `canvas/src/server/watcher.ts`:

Add imports for the new functions:
- `import { seedDesignRulesIfEmpty, seedGlobalVisualStyleIfEmpty } from './brand-seeder'`
- `import { getDesignRules, getDesignRule, updateDesignRule, getDesignRulesByArchetype } from './db-api'`

In the server startup/config function (where `seedVoiceGuideIfEmpty` and `seedBrandPatternsIfEmpty` are called), add calls to:
- `await seedGlobalVisualStyleIfEmpty()`
- `await seedDesignRulesIfEmpty()`

Add API routes (following existing patterns in the file):

GET `/api/design-rules` — accepts optional query params `scope` and `platform`. Returns JSON array of DesignRule objects.

GET `/api/design-rules/:id` — returns single DesignRule by ID.

PUT `/api/design-rules/:id` — accepts JSON body `{ content: string }`, calls `updateDesignRule(id, content)`, returns `{ ok: true }`.

GET `/api/design-rules/archetype/:slug` — accepts optional query param `platform`. Returns archetype-specific design rules via `getDesignRulesByArchetype(slug, platform)`.
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS/canvas && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - canvas/src/server/brand-seeder.ts contains `export async function seedGlobalVisualStyleIfEmpty`
    - canvas/src/server/brand-seeder.ts contains `export async function seedDesignRulesIfEmpty`
    - canvas/src/server/brand-seeder.ts contains 'visual-compositor-contract' slug string
    - canvas/src/server/brand-seeder.ts contains 'problem-first' archetype_slug string
    - canvas/src/server/brand-seeder.ts contains at least 7 archetype entries (problem-first, quote, stat-proof, app-highlight, manifesto, partner-alert, feature-spotlight)
    - canvas/src/server/watcher.ts contains `seedDesignRulesIfEmpty` import
    - canvas/src/server/watcher.ts contains `seedGlobalVisualStyleIfEmpty` import
    - canvas/src/server/watcher.ts contains `/api/design-rules` route string
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Global visual style rules seeded into brand_patterns as 'visual-style' category, 10 design rule rows seeded into template_design_rules (1 global-social + 2 platform + 7 archetype), 4 API endpoints created, all TypeScript compiles clean</done>
</task>

</tasks>

<verification>
1. TypeScript compiles: `cd canvas && npx tsc --noEmit` exits 0
2. DB table created: Start the dev server, verify template_design_rules table exists
3. Seeder runs: On first startup, global visual style rule appears in brand_patterns, 10 design rules appear in template_design_rules
4. API serves data: `curl localhost:5173/api/design-rules` returns JSON array with 10 entries
</verification>

<success_criteria>
- template_design_rules table with scope/platform/archetype_slug columns exists in schema
- Global visual compositor contract seeded into brand_patterns (category: visual-style)
- 10 design rules seeded: 1 global-social + 2 platform + 7 archetype
- 4 API endpoints functional: GET list, GET by id, PUT update, GET by archetype
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/14-design-dna-template-extracted-style-rules-per-deliverable-design-intelligence-and-exemplar-referenced-generation-pipeline/14-01-SUMMARY.md`
</output>
