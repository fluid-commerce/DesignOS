---
phase: 06-marketing-skills-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/marketing/  # 30 skill directories copied recursively
  - brand/skill-map.json
autonomous: false
requirements: [MKTG-03, MKTG-05]
user_setup: []

must_haves:
  truths:
    - "skills/marketing/ contains all 30 marketing skill directories with SKILL.md files"
    - "Skills with references/ subdirectories have their references copied too"
    - "brand/skill-map.json exists with operator-approved mappings for 12 slots (3 asset types x 4 roles)"
    - "skill-map.json entries resolve to real files in skills/marketing/"
    - "No more than 2 skills per subagent slot in skill-map.json"
  artifacts:
    - path: "skills/marketing/"
      provides: "30 marketing skill directories"
    - path: "brand/skill-map.json"
      provides: "Skill-to-task mapping manifest"
      contains: "social-post"
  key_links:
    - from: "brand/skill-map.json"
      to: "skills/marketing/*/SKILL.md"
      via: "file path references"
      pattern: "skills/marketing/.*/SKILL\\.md"
---

<objective>
Copy 30 marketing domain skills into this repo and create the operator-approved skill-map.json manifest.

Purpose: Establishes the skill library and mapping registry that orchestrators will reference in Plan 02. The interactive walkthrough ensures the operator curates which skills load for each asset type + role combination.

Output: `skills/marketing/` directory with 30 skill subdirectories, `brand/skill-map.json` with 12 approved mapping entries.
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-marketing-skills-integration/06-CONTEXT.md
@.planning/phases/06-marketing-skills-integration/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy 30 marketing skills into skills/marketing/</name>
  <files>skills/marketing/ (30 subdirectories)</files>
  <action>
Copy all 30 marketing domain skill directories from ~/.agents/skills/ into skills/marketing/ in this repo. Use recursive copy (cp -r) to capture SKILL.md files AND any references/ subdirectories.

The 30 marketing skills to copy (confirmed from research):
copywriting, copy-editing, social-content, content-strategy, marketing-psychology, page-cro, popup-cro, signup-flow-cro, onboarding-cro, form-cro, paywall-upgrade-cro, churn-prevention, seo-audit, schema-markup, ai-seo, programmatic-seo, site-architecture, analytics-tracking, ab-test-setup, sales-enablement, cold-email, competitor-alternatives, ad-creative, paid-ads, email-sequence, launch-strategy, free-tool-strategy, pricing-strategy, referral-program, marketing-ideas, revops

For each skill:
1. Create skills/marketing/{name}/ directory
2. Copy the entire directory recursively: cp -r ~/.agents/skills/{name}/ skills/marketing/{name}/
3. Verify SKILL.md exists in the copied directory

Do NOT copy non-marketing skills (brand-intelligence, fluid-social, fluid-one-pager, fluid-theme-section, fluid-design-os, brand-compliance-check, scaffold-section, design-variations, frontend-design, gemini-image-gen, google-stitch, gradient-bro, plan-worktree-workflow, prototype-handoff, ui-ux-pro-max, agentation, agentation-self-driving, generate-video, slidev, upscale-video, video-to-context, query-fluid-backend, query-fluid-mono, product-marketing-context).

After copying, list all skills/marketing/*/ directories and confirm count is 30.
  </action>
  <verify>
    <automated>ls skills/marketing/ | wc -l | tr -d ' ' | grep -x 30 && echo "PASS: 30 skills copied" || echo "FAIL: expected 30 skills"</automated>
  </verify>
  <done>skills/marketing/ contains exactly 30 marketing skill directories, each with at least a SKILL.md file. Skills with references/ subdirectories have those copied too.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Interactive skill-map.json curation walkthrough</name>
  <files>brand/skill-map.json</files>
  <action>
Present the proposed skill-to-task mapping to the operator for approval. Walk through each asset type group with rationale and ask for approval or modification.

The skill-map.json manifest defines the default marketing skills loaded per subagent for each asset type. 3 asset types (social-post, website-section, one-pager) x 4 subagent roles (copy-agent, layout-agent, styling-agent, spec-check-agent) = 12 mapping slots.

Constraint: 1-2 marketing skills max per subagent slot (token budget).
Brand docs are PRIMARY; marketing skills are SECONDARY reference.
Layout-agent and styling-agent likely get no marketing skills (marketing skills operate at copy/strategy/validation level, not CSS/layout level).

Research-proposed defaults (from 06-RESEARCH.md):

**social-post:**
- copy-agent: copywriting + social-content (platform-specific content strategy)
- layout-agent: [] (brand archetypes govern layout)
- styling-agent: [] (design tokens govern styling)
- spec-check-agent: analytics-tracking (UTM/tracking awareness)

**one-pager:**
- copy-agent: copywriting + sales-enablement ("sales uses what sales trusts")
- layout-agent: []
- styling-agent: []
- spec-check-agent: [] (brand compliance sufficient)

**website-section:**
- copy-agent: copywriting + page-cro (value prop clarity, CTA hierarchy)
- layout-agent: []
- styling-agent: []
- spec-check-agent: seo-audit (on-page SEO checklist)

18 skills that don't map to any asset type (standalone tools only): ab-test-setup, email-sequence, cold-email, paid-ads, popup-cro, signup-flow-cro, form-cro, paywall-upgrade-cro, onboarding-cro, churn-prevention, programmatic-seo, site-architecture, free-tool-strategy, launch-strategy, pricing-strategy, referral-program, marketing-ideas, revops

Walk through steps:
1. Present each asset-type group with the proposed skills and rationale
2. For each slot, ask: "Approve? (y/n/alternative)"
3. Present the standalone-only skills list and confirm
4. After all approvals, write the final brand/skill-map.json

After operator approves, write brand/skill-map.json with the approved mappings using this schema:

```json
{
  "_meta": {
    "version": "1.0",
    "updated": "{today's date}",
    "note": "1-2 skills per subagent max. Brand docs take precedence over all marketing skills."
  },
  "social-post": {
    "copy-agent": ["skills/marketing/{approved}/SKILL.md", ...],
    "layout-agent": [],
    "styling-agent": [],
    "spec-check-agent": [...]
  },
  "one-pager": { ... },
  "website-section": { ... }
}
```

After writing, validate: JSON parses, all paths resolve, no slot exceeds 2 entries.
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS && node -e "const m=JSON.parse(require('fs').readFileSync('brand/skill-map.json','utf8')); const types=['social-post','one-pager','website-section']; const roles=['copy-agent','layout-agent','styling-agent','spec-check-agent']; let ok=true; types.forEach(t=>roles.forEach(r=>{const s=m[t][r]; if(!Array.isArray(s)){console.log('FAIL: missing '+t+'/'+r);ok=false;} if(s.length>2){console.log('FAIL: >2 skills at '+t+'/'+r);ok=false;} s.forEach(p=>{if(!require('fs').existsSync(p)){console.log('FAIL: missing file '+p);ok=false;}})})); console.log(ok?'PASS':'FAIL')"</automated>
  </verify>
  <done>brand/skill-map.json exists with valid JSON, all file paths resolve to real files, no slot exceeds 2 skills, operator has approved every mapping.</done>
</task>

</tasks>

<verification>
- `ls skills/marketing/ | wc -l` returns 30
- `node -e "JSON.parse(require('fs').readFileSync('brand/skill-map.json','utf8'))"` succeeds
- All skill paths in skill-map.json resolve to existing files
- No mapping slot has more than 2 entries
</verification>

<success_criteria>
- 30 marketing skill directories exist under skills/marketing/ with SKILL.md files
- brand/skill-map.json contains operator-approved mappings for all 12 slots
- All file paths in the manifest are valid
</success_criteria>

<output>
After completion, create `.planning/phases/06-marketing-skills-integration/06-01-SUMMARY.md`
</output>
