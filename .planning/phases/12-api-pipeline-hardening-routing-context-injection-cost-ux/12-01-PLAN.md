---
phase: 12-api-pipeline-hardening-routing-context-injection-cost-ux
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - canvas/src/server/watcher.ts
  - canvas/src/__tests__/generate-endpoint.test.ts
  - canvas/src/__tests__/api-pipeline.test.ts
autonomous: true
requirements: [CLEAN-01, CLEAN-02]

must_haves:
  truths:
    - "CLI generation paths (iterate + campaign) are fully removed from watcher.ts"
    - "No references to spawn('claude', ...) remain in the server codebase"
    - "The /api/generate endpoint only supports API mode (no engine='cli' branch)"
    - "The /api/generate/cancel endpoint works without CLI child process references"
    - "All tests pass after CLI code removal"
  artifacts:
    - path: "canvas/src/server/watcher.ts"
      provides: "Server middleware with CLI dead code removed"
      contains: "runApiPipeline"
    - path: "canvas/src/__tests__/api-pipeline.test.ts"
      provides: "Pipeline tests updated to remove CLI engine routing tests"
  key_links:
    - from: "canvas/src/server/watcher.ts"
      to: "canvas/src/server/api-pipeline.ts"
      via: "runApiPipeline import"
      pattern: "import.*runApiPipeline"
---

<objective>
Remove all CLI-era generation code from the server codebase. The `claude -p` spawn paths in watcher.ts (iterate mode and campaign CLI mode) are dead code since Phase 10 made API the default and Phase 11 hardened it. This plan removes ~250 lines of CLI spawning, child process tracking, and associated imports, then cleans up tests that were testing CLI-specific behavior.

Purpose: Eliminate dead code paths that add maintenance burden and obscure the actual generation flow.
Output: Cleaner watcher.ts with only API pipeline generation, updated tests.
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-api-pipeline-hardening-routing-context-injection-cost-ux/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove CLI generation paths from watcher.ts</name>
  <files>canvas/src/server/watcher.ts</files>
  <read_first>
    - canvas/src/server/watcher.ts (full file — understand all CLI references before removing)
    - canvas/src/server/api-pipeline.ts (first 15 lines — confirm runApiPipeline import is the only generation path needed)
  </read_first>
  <action>
Remove all CLI-era generation code from watcher.ts. Specifically:

**1. Remove imports that are only used by CLI paths:**
- `spawn, type ChildProcess` from `node:child_process` (line 3)
- `createReadStream` from `node:fs` (line 8) — unused entirely
- `createInterface` from `node:readline` (line 9) — only used in CLI spawn stdout/stderr parsing

**2. Remove CLI state variables (lines 170-173):**
- `let activeChild: ChildProcess | null = null;` (line 171)
- `const activeChildren: Map<string, ChildProcess> = new Map();` (line 173)
- Keep `let activeCampaignGeneration: string | null = null;` (line 174) — used by API mode too

**3. Remove the entire ITERATE MODE block (~lines 1232-1359):**
This is the `if (isIteration) { ... }` block that spawns `claude -p` for single-asset iteration. The entire block from `if (isIteration) {` through the `return;` before `// NEW CAMPAIGN MODE` should be removed. Also remove the `isIteration` detection variables above it:
- `const { sessionId: reqSessionId, iterationContext } = body;` (line 1229)
- `const isIteration = !!(reqSessionId && iterationContext);` (line 1230)

**4. Remove the CLI MODE branch in campaign generation (~lines 1473-1611):**
The `if (engine === 'cli') { ... } else { ... }` block should be collapsed. Remove the entire `if (engine === 'cli') { ... }` branch (lines 1473-1611). Keep only the API MODE contents (lines 1613+), but remove the `else {` wrapper so it becomes the default flow. Also remove:
- `const engine = (body.engine as string | undefined) ?? 'api';` (line 1364)

**5. Simplify the /api/generate/cancel endpoint (~lines 1198-1221):**
Remove CLI child process killing code. The simplified cancel handler should only:
- Clear `activeCampaignGeneration`
- Return the JSON response
Remove references to `activeChildren` and `activeChild` from this handler.

**6. Verify no other references to removed variables remain.**
Search for `activeChild`, `activeChildren`, `ChildProcess`, `createInterface`, `createReadStream` — all should be gone.
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS && grep -qE 'spawn|activeChild|ChildProcess|createInterface|createReadStream|engine.*cli' canvas/src/server/watcher.ts && echo 'FAIL: CLI refs remain' || echo 'PASS: no CLI refs'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "spawn" canvas/src/server/watcher.ts` returns 0 (no spawn references)
    - `grep -c "activeChild" canvas/src/server/watcher.ts` returns 0 (no child process tracking)
    - `grep -c "ChildProcess" canvas/src/server/watcher.ts` returns 0 (no type import)
    - `grep -c "createInterface" canvas/src/server/watcher.ts` returns 0 (no readline)
    - `grep -c "createReadStream" canvas/src/server/watcher.ts` returns 0 (no read stream)
    - `grep -c "engine.*cli" canvas/src/server/watcher.ts` returns 0 (no CLI engine branch)
    - `grep "runApiPipeline" canvas/src/server/watcher.ts` still finds the API pipeline call
    - `grep "activeCampaignGeneration" canvas/src/server/watcher.ts` still finds the campaign lock (used by API mode)
    - `import { spawn` line is removed from the file
    - The file still compiles (no TypeScript errors from removed references)
  </acceptance_criteria>
  <done>watcher.ts contains zero CLI generation code — no spawn, no child process tracking, no engine routing. Only the API pipeline path remains. The iterate mode block is fully removed. The campaign generation only uses runApiPipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up CLI-referencing tests</name>
  <files>canvas/src/__tests__/generate-endpoint.test.ts, canvas/src/__tests__/api-pipeline.test.ts</files>
  <read_first>
    - canvas/src/__tests__/generate-endpoint.test.ts (full file — entirely about CLI spawn behavior, likely needs full deletion)
    - canvas/src/__tests__/api-pipeline.test.ts (lines 660-680 — engine routing tests that reference CLI)
  </read_first>
  <action>
**1. Delete canvas/src/__tests__/generate-endpoint.test.ts entirely.**
This file tests CLI spawn behavior (spawn('claude', ...), activeChild tracking, activeChildren Map, stdin stdio options). All of these code paths no longer exist after Task 1. The entire test file is dead.

**2. Update canvas/src/__tests__/api-pipeline.test.ts:**
Find the "Engine routing" test section (~line 663). Remove or update tests that reference `engine: 'cli'`:
- Remove the test `'body.engine="cli" enables CLI path'` — CLI path no longer exists
- Keep any test that validates API as the default engine, but update the description to reflect there's only one engine now
- If the engine routing describe block only contains CLI tests, remove the entire describe block

Also check: the `vi.mock('node:child_process', ...)` at the top of api-pipeline.test.ts (~line 17-19). This mock is used by the `run_brand_check` tool tests (which use `execSync` to run brand-compliance.cjs). Keep this mock — it's not CLI generation code, it's for the spec-check tool executor. Do NOT remove it.

**3. Run the full test suite to verify nothing is broken:**
`cd canvas && npx vitest run --reporter=verbose`
  </action>
  <verify>
    <automated>cd /Users/cheyrasmussen/Fluid-DesignOS/canvas && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `canvas/src/__tests__/generate-endpoint.test.ts` does not exist (deleted)
    - `grep -c "engine.*cli" canvas/src/__tests__/api-pipeline.test.ts` returns 0
    - The `vi.mock('node:child_process', ...)` mock in api-pipeline.test.ts still exists (needed for execSync/brand-check tests)
    - `cd canvas && npx vitest run` shows no NEW failures (pre-existing failures in AppShell/VoiceGuide/prompt-sidebar are acceptable)
    - Test count is lower than before (generate-endpoint tests removed)
  </acceptance_criteria>
  <done>All CLI-specific tests removed. generate-endpoint.test.ts deleted. api-pipeline.test.ts engine routing CLI tests removed. Full test suite passes with no new failures.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn "spawn\|claude.*-p\|activeChild\|ChildProcess" canvas/src/server/watcher.ts` returns nothing
2. `ls canvas/src/__tests__/generate-endpoint.test.ts` returns "No such file"
3. `cd canvas && npx vitest run` passes (no new failures vs pre-existing baseline of ~9 failures)
4. `wc -l canvas/src/server/watcher.ts` should be ~200-250 lines shorter than current 1951
</verification>

<success_criteria>
- watcher.ts has no CLI generation code (spawn, child process, iterate CLI, campaign CLI)
- All imports related to CLI paths removed (child_process spawn, readline, fs createReadStream)
- Tests pass with no new failures
- The /api/generate endpoint works exclusively via runApiPipeline
</success_criteria>

<output>
After completion, create `.planning/phases/12-api-pipeline-hardening-routing-context-injection-cost-ux/12-01-SUMMARY.md`
</output>
