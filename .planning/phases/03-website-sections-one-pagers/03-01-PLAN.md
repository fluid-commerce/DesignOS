---
phase: 03-website-sections-one-pagers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/fluid-themes-gold-standard/EMPIRICAL-FINDINGS.md
  - docs/fluid-themes-gold-standard/schema-rules.md
  - docs/fluid-themes-gold-standard/button-system.md
  - docs/fluid-themes-gold-standard/template-patterns.md
  - docs/fluid-themes-gold-standard/validation-checklist.md
  - docs/fluid-themes-gold-standard/theme-tokens.md
autonomous: true
requirements: [SITE-02]

must_haves:
  truths:
    - "Layout agent can load schema-rules.md and understand how to structure a .liquid section schema"
    - "Styling agent can load button-system.md and theme-tokens.md and understand all utility class patterns"
    - "Spec-check agent can load validation-checklist.md and know exactly what to validate"
    - "Empirical findings document how schema renders into editor sidebar controls"
  artifacts:
    - path: "docs/fluid-themes-gold-standard/EMPIRICAL-FINDINGS.md"
      provides: "Empirical research on Fluid theme rendering pipeline"
      contains: "schema_tag"
    - path: "docs/fluid-themes-gold-standard/schema-rules.md"
      provides: "Schema planning rules for layout agent"
      contains: "13 font sizes"
    - path: "docs/fluid-themes-gold-standard/button-system.md"
      provides: "Button utility class system for styling agent"
      contains: "btn btn-"
    - path: "docs/fluid-themes-gold-standard/template-patterns.md"
      provides: "Template implementation patterns for layout agent"
      contains: "block.fluid_attributes"
    - path: "docs/fluid-themes-gold-standard/validation-checklist.md"
      provides: "Validation checklist for spec-check agent"
      contains: "Schema Validation"
    - path: "docs/fluid-themes-gold-standard/theme-tokens.md"
      provides: "CSS variable and utility class reference for styling agent"
      contains: "var(--clr-"
  key_links:
    - from: "docs/fluid-themes-gold-standard/schema-rules.md"
      to: "tools/rules.json"
      via: "option count references"
      pattern: "13 font sizes.*13 colors.*5 weights"
    - from: "docs/fluid-themes-gold-standard/EMPIRICAL-FINDINGS.md"
      to: ".claude/agents/layout-agent.md"
      via: "pipeline understanding informs agent instructions"
      pattern: "SchemaTag|fluid_attributes"
---

<objective>
Research the Fluid theme rendering pipeline empirically and decompose the Gold Standard workflow document into role-mapped files that agents can load per their contracts.

Purpose: Every downstream task (hero PoC, section templates, agent upgrades) depends on understanding how the theme system actually works and having focused docs that each agent can load without pulling the entire Gold Standard monolith.

Output: 6 files in docs/fluid-themes-gold-standard/ -- 1 empirical research summary + 5 role-mapped Gold Standard decomposition files.
</objective>

<execution_context>
@/Users/cheyrasmussen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/cheyrasmussen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-website-sections-one-pagers/03-CONTEXT.md
@.planning/phases/03-website-sections-one-pagers/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Empirical research of Fluid theme rendering pipeline</name>
  <files>docs/fluid-themes-gold-standard/EMPIRICAL-FINDINGS.md</files>
  <action>
Investigate the Fluid backend and frontend repos to document how the theme system actually works end-to-end. This is the highest-priority research per user decisions.

**Research queries (all from CONTEXT.md locked decisions):**

1. **Schema to editor sidebar pipeline**: Read `/Users/cheyrasmussen/fluid/app/themes/liquid_tags/schema_tag.rb` and trace how the parsed JSON schema becomes editor sidebar controls. Follow the code path from `SchemaTag` through to the frontend that renders the settings panel.

2. **Liquid to DOM rendering**: Read `/Users/cheyrasmussen/fluid/app/services/themes/templates/renderer.rb` and trace how .liquid templates get compiled into final HTML. Document the transformation steps.

3. **Theme variable resolution**: Read `/Users/cheyrasmussen/fluid/app/themes/` directory and Grep for `var(--` or `settings.` patterns to understand how theme-level variables (colors, fonts, spacing) resolve in section templates.

4. **Block rendering**: Read `/Users/cheyrasmussen/fluid/app/models/application_theme_template.rb` focusing on lines 294-324 and 512-524 to document how `block.fluid_attributes` generates data attributes, how blocks are reordered via `block_order`, and what `fluid_attributes` actually outputs.

5. **Utility class system**: Grep `/Users/cheyrasmussen/fluid-mono` for CSS files containing `text-xs`, `text-sm`, `bg-primary`, `btn-filled`, `py-xs`, `rounded-sm` class definitions to document the actual available utility classes.

6. **Find a working section**: Look at 2-3 sections in `/Users/cheyrasmussen/fluid/app/themes/templates/global/sections/` to understand the current template structure (even though they're not Gold Standard, understanding the existing pattern helps).

7. **How `{% render 'section-css' %}` resolves**: Search for `section-css` in the themes directory to understand CSS inclusion patterns.

**Output format:** Write `docs/fluid-themes-gold-standard/EMPIRICAL-FINDINGS.md` with these sections:
- Schema Rendering Pipeline (how schema JSON becomes editor controls)
- Template Rendering Pipeline (how .liquid becomes HTML)
- Theme Variable Resolution (how var(--clr-*) etc. resolve)
- Block System (fluid_attributes, block_order, block types)
- Available Utility Classes (confirmed from CSS source)
- Section CSS Pattern (how section-css renders)
- Working Section Analysis (what existing sections look like)
- Open Questions (anything unresolved)

Each section: what was found, where (file paths + line numbers), confidence level.

**CRITICAL: Do NOT rewrite or interpret -- document what the code actually does.**
  </action>
  <verify>
    <automated>test -f docs/fluid-themes-gold-standard/EMPIRICAL-FINDINGS.md && grep -c "Schema Rendering Pipeline\|Template Rendering Pipeline\|Block System\|Utility Classes" docs/fluid-themes-gold-standard/EMPIRICAL-FINDINGS.md | grep -q "[4-9]" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>EMPIRICAL-FINDINGS.md exists with at least 4 major sections documenting the actual Fluid theme rendering pipeline from source code investigation</done>
</task>

<task type="auto">
  <name>Task 2: Decompose Gold Standard workflow into role-mapped files</name>
  <files>docs/fluid-themes-gold-standard/schema-rules.md, docs/fluid-themes-gold-standard/button-system.md, docs/fluid-themes-gold-standard/template-patterns.md, docs/fluid-themes-gold-standard/validation-checklist.md, docs/fluid-themes-gold-standard/theme-tokens.md</files>
  <action>
Split the Gold Standard workflow document into 5 role-mapped files. Per user locked decision: **preserve exact original wording -- split, do NOT rewrite.**

**Source:** Read `Reference/Fluid Themes/GOLD_STANDARD_WORKFLOW.md` (the monolithic source of truth, Weight 100).

**Decomposition map (from RESEARCH.md):**

| Target File | Agent Consumer | Source Sections |
|-------------|---------------|-----------------|
| `schema-rules.md` | Layout agent | Phase 1 (Planning) + Phase 2 Step 1 (Schema First): Schema planning checklist, setting order (Content -> Interactive -> Layout -> Container), text element 6-setting rule, option counts (13 font sizes, 13 colors, 5 weights) |
| `button-system.md` | Styling agent | Phase 2 Step 3 (Button Implementation) + Phase 3 button validation: Button 7-setting rule, btn utility classes, NEVER custom button styles |
| `template-patterns.md` | Layout agent | Phase 2 Step 2 (Template Implementation): Utility class usage in liquid templates, block.fluid_attributes, image placeholders, section/container wrapper patterns |
| `validation-checklist.md` | Spec-check agent | Phase 4 (Validation) full checklist: Schema validation, template validation, default values, CSS validation, quality checks |
| `theme-tokens.md` | Styling agent | Phase 2 references + Critical Rules: CSS variable patterns (var(--clr-*), var(--space-*), var(--radius-*)), no-hardcode rules, all 13 color names, all 13 size names, all 5 weight names |

**Rules:**
- Copy exact paragraphs/sentences from the Gold Standard doc into the appropriate file
- Add a header with: `Source: GOLD_STANDARD_WORKFLOW.md` and `Agent: {agent-name}` and `Weight: 100`
- If a paragraph is relevant to multiple agents, duplicate it into both files (better than cross-references for agent context loading)
- Add cross-references at the bottom of each file pointing to related docs (wiki-link format per Phase 1 pattern)
- Supplement with confirmed data from `tools/rules.json` where the Gold Standard references option counts -- include the actual option values inline so agents don't need to also load rules.json
- Include findings from EMPIRICAL-FINDINGS.md where they clarify how a Gold Standard rule actually manifests in code (e.g., how block.fluid_attributes renders)

**Weight annotations:** Add `(Weight: 100 -- Gold Standard)` to critical rules so agents know enforcement level.
  </action>
  <verify>
    <automated>for f in docs/fluid-themes-gold-standard/schema-rules.md docs/fluid-themes-gold-standard/button-system.md docs/fluid-themes-gold-standard/template-patterns.md docs/fluid-themes-gold-standard/validation-checklist.md docs/fluid-themes-gold-standard/theme-tokens.md; do test -f "$f" && echo "EXISTS: $f" || echo "MISSING: $f"; done</automated>
  </verify>
  <done>All 5 role-mapped Gold Standard files exist, each contains exact wording from the source document with agent role headers and weight annotations, and cross-references to related docs</done>
</task>

</tasks>

<verification>
- All 6 files exist in docs/fluid-themes-gold-standard/
- EMPIRICAL-FINDINGS.md documents actual code paths with file references
- Each decomposed file references its source and target agent
- No content is lost from the Gold Standard doc (coverage check: all major sections assigned to at least one file)
- Gold Standard wording is preserved, not rewritten
</verification>

<success_criteria>
- 6 files in docs/fluid-themes-gold-standard/ covering empirical research + 5 decomposed Gold Standard roles
- Layout agent context: schema-rules.md + template-patterns.md
- Styling agent context: button-system.md + theme-tokens.md
- Spec-check agent context: validation-checklist.md
- EMPIRICAL-FINDINGS.md answers all 7 research queries from CONTEXT.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-website-sections-one-pagers/03-01-SUMMARY.md`
</output>
