---
phase: 14.1-brand-agnostic-pipeline
plan: 02
subsystem: skills
tags: [cli-skills, api-wrapper, sse-streaming, skill-management]

requires:
  - phase: 14.1-01
    provides: Brand-agnostic buildSystemPrompt and api-pipeline.ts with no hardcoded Fluid refs

provides:
  - Three thin API wrapper skills (generate-social, generate-one-pager, generate-theme-section) that POST to /api/generate
  - Old orchestrator skills (fluid-social, fluid-one-pager, fluid-theme-section) deleted
  - fluid-design-os usage example updated to reference new skill name

affects: [14.1-03, any docs or skills referencing old fluid-social/fluid-one-pager/fluid-theme-section skill names]

tech-stack:
  added: []
  patterns: [Thin CLI wrapper skill — parse args, POST /api/generate, stream SSE response, no pipeline logic in skill]

key-files:
  created:
    - "~/.agents/skills/generate-social/SKILL.md"
    - "~/.agents/skills/generate-one-pager/SKILL.md"
    - "~/.agents/skills/generate-theme-section/SKILL.md"
  modified:
    - "~/.agents/skills/fluid-design-os/SKILL.md (updated /fluid-social usage example to /generate-social)"

key-decisions:
  - "fluid-campaign skill does not exist — no update needed; research note confirmed 0 matches were expected"
  - "generate-social, generate-one-pager, generate-theme-section all use localhost:5174 (not 5173 from plan template) matching actual app port"
  - "fluid-design-os/SKILL.md /fluid-social usage hint updated as Rule 1 auto-fix since old skill no longer exists"

patterns-established:
  - "Thin CLI wrapper: SKILL.md under 50 lines, zero brand/ refs, zero pipeline logic, POST /api/generate with SSE stream"

requirements-completed: [SC-3]

duration: 2min
completed: 2026-03-17
---

# Phase 14.1 Plan 02: CLI Orchestrators to Thin API Wrappers Summary

**Three CLI orchestrator skills replaced with thin POST /api/generate wrappers (~40 lines each), old 250+ line orchestrators deleted, all old skill name references removed from agent skill ecosystem**

## Performance

- **Duration:** 2 min
- **Started:** 2026-03-17T20:39:32Z
- **Completed:** 2026-03-17T20:41:32Z
- **Tasks:** 2
- **Files modified:** 4 (3 created, 1 updated) — all outside git repo

## Accomplishments

- Created `generate-social`, `generate-one-pager`, `generate-theme-section` as thin API wrappers (40-45 lines each, zero brand/ refs, zero pipeline logic)
- Deleted `fluid-social`, `fluid-one-pager`, `fluid-theme-section` directories (250+ line orchestrators)
- Confirmed `fluid-campaign` skill does not exist — no stale references possible
- Fixed `fluid-design-os/SKILL.md` usage hint that still referenced `/fluid-social`
- Ran `sync.sh` twice (after Task 1 deletions/creates, after fluid-design-os fix); 56 skills synced

## Task Commits

These tasks created/modified files outside the git repo (at `~/.agents/skills/`). No in-repo files changed.

1. **Task 1: Create thin API wrapper skills and delete old orchestrator skills** — external files only (no git commit for skill files per plan instructions)
2. **Task 2: Verify/update fluid-campaign references** — confirmed skill does not exist; no changes needed

**Plan metadata:** committed with docs commit below

## Files Created/Modified

- `~/.agents/skills/generate-social/SKILL.md` — Thin wrapper: parse args, check server, POST /api/generate {prompt, creationType: platform}, stream SSE response
- `~/.agents/skills/generate-one-pager/SKILL.md` — Thin wrapper: parse args, check server, POST /api/generate {prompt, creationType: "one-pager"}, stream SSE response
- `~/.agents/skills/generate-theme-section/SKILL.md` — Thin wrapper: parse args, check server, POST /api/generate {prompt, creationType: "theme-section"}, stream SSE response
- `~/.agents/skills/fluid-design-os/SKILL.md` — Updated echo usage hint from /fluid-social to /generate-social

## Decisions Made

- **fluid-campaign skill absent**: Research noted it "reported 0 matches" — confirmed it does not exist at `~/.agents/skills/fluid-campaign/`. Task 2 acceptance criteria trivially passes.
- **Port 5174 not 5173**: The plan template showed localhost:5173 but the actual app runs on 5174 (confirmed by other skill files and watcher.ts configuration). Used 5174 throughout.
- **fluid-design-os auto-fix**: The `/fluid-social` usage hint in fluid-design-os/SKILL.md would show users a non-existent command. Fixed as Rule 1 (broken behavior in related skill file triggered by this plan's deletions).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated /fluid-social reference in fluid-design-os/SKILL.md**
- **Found during:** Task 2 verification (grep for old skill names)
- **Issue:** `fluid-design-os/SKILL.md` had `echo "  Generate assets:  /fluid-social \"your topic\" --variations 3"` — this usage hint would now point users to a deleted skill
- **Fix:** Updated the echo string to reference `/generate-social` instead
- **Files modified:** `~/.agents/skills/fluid-design-os/SKILL.md`
- **Verification:** `grep -r "fluid-social|fluid-one-pager|fluid-theme-section" ~/.agents/skills/` returns zero matches
- **Committed in:** Skill file outside git, sync.sh re-run after fix

---

**Total deviations:** 1 auto-fixed (Rule 1 — stale reference in usage hint)
**Impact on plan:** Necessary correctness fix. No scope creep.

## Issues Encountered

None — both tasks executed cleanly. The fluid-campaign absence was already anticipated by the plan's "NOTE: The research doc reported 0 matches..." caveat.

## User Setup Required

None — no external service configuration required. Skills are distributed via `~/.agents/sync.sh` which ran successfully.

## Next Phase Readiness

- Phase 14.1 Plan 03 (cleanup: brand-intelligence skill rewrite, CLAUDE.md update, `git rm -r brand/`) can proceed
- All old CLI skill references are eliminated from the agent ecosystem
- The three new thin wrappers are live and registered via sync.sh

---
*Phase: 14.1-brand-agnostic-pipeline*
*Completed: 2026-03-17*
